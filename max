You are working in the GitHub repository hiyroscript/alva on the main branch.

Make a focused visual cleanup to the Select Fighter screen only. Do not redesign the screen and do not alter the game’s underlying fighter data, sprite assets, loading system, selection flow, combat systems, or other screens.

Goal

Simplify the Select Fighter screen by removing the extra developer-style metadata, animation controls, floor marker, roster count, and bottom control hints. The result should feel cleaner and more presentation-focused while preserving the existing roster, fighter preview, selection behavior, availability state, and Confirm action.

The relevant implementation is primarily in:

* js/screens/character-select-screen.js
* styles.css
* ALVA_SPEC.md

The shared hint component lives in:

* js/ui/components.js

Do not remove or break the shared hint component globally. Other screens may still use it.

Required removals

On the Select Fighter screen, completely remove the following visible UI:

1. The control hints in the bottom-right/bottom footer:
    * arrow keys + Navigate
    * Enter + Select
    * Esc + Back
2. The roster availability count:
    * 1 / 48 available
    * or any equivalent availability-count metadata beside the Roster heading.
3. The thin horizontal floor/ground line underneath the fighter in the preview.
4. The preview subtext:
    * Roster slot 01
5. The animation section label:
    * Animations
6. The animation controls/chips:
    * Idle
    * Run
7. The frame metadata:
    * Idle frames
    * 4
    * Run frames
    * 6
8. The attack metadata:
    * Attack set
    * Awaiting sprites

Do not leave empty containers, blank rows, unnecessary gaps, invisible focus targets, or spacing where these elements used to be.

js/screens/character-select-screen.js

The current file constructs all of these elements directly.

Bottom control hints

The file currently imports:

screenHeader, hintBar, MENU_HINTS

from ../ui/components.js.

The screen then adds:

hintBar(MENU_HINTS)

as the final child in this.el.replaceChildren(...).

Remove the hint bar from CharacterSelectScreen only.

Afterward, clean up the unused hintBar and MENU_HINTS imports from this file.

Do not modify the shared hintBar() implementation in js/ui/components.js, because other screens may still rely on it.

Roster availability count

The file calculates:

const available = CHARACTERS.filter((c) => c.available).length;

and renders a .panel-meta containing the available/total count.

Remove the .panel-meta availability element from the roster header.

Keep the Roster title.

If the available variable becomes unused, remove it.

Do not change CONFIG.roster.totalSlots, the 48-slot roster, available/locked logic, or character definitions.

Preview floor line

The preview currently renders:

<div class="preview-floor">

inside .preview-stage.

Delete this element completely.

The fighter canvas should remain unchanged and continue to occupy the preview stage normally.

Roster slot text

For available fighter #0001, the preview currently assigns:

Roster slot 01

to the .preview-sub element.

Remove this visible roster-slot metadata.

Do not replace it with another slot number, ID description, subtitle, or filler text.

If .preview-sub is no longer required for the available fighter state, avoid leaving an empty <p> that reserves space.

The locked-slot explanatory message may be retained if useful, but it must only exist/render for an actual locked-slot preview. Do not preserve an empty subtitle underneath #0001.

Animation UI

The file currently creates:

* this.chips
* .preview-anims
* the Animations label
* clickable Idle and Run chips
* ANIM_LABELS

Remove the animation-control UI completely.

There should be no visible Animations, Idle, or Run text in the Select Fighter preview.

Do not delete the actual idle/run animation definitions from js/data/characters.js.

Do not delete the sprite frames.

Do not modify the normalization or sprite-loading system.

The preview should continue displaying an animated fighter. With the manual animation selector gone, keeping the existing idle animation as the default preview is appropriate.

Simplify the code accordingly. If ANIM_LABELS, animation-chip event handlers, or animation-selection code becomes unused, remove the dead code.

Do not fabricate replacement controls.

Frame facts and attack set

The file currently creates:

this.facts = el('dl', { class: 'detail-list detail-list--compact' })

and fills it with:

* Idle frames → 4
* Run frames → 6
* Attack set → Awaiting sprites

Remove this entire metadata/facts block from the Select Fighter preview.

Do not merely hide the labels while leaving the values.

Do not leave 4, 6, or Awaiting sprites visible by themselves.

Remove any now-unused DOM construction and update logic associated with this facts list.

What must remain

The Select Fighter screen must still retain its important gameplay UI and behavior:

* Select Fighter screen title
* Quick Battle setup header/step indicator
* normal Back button in the header
* Roster heading
* all 48 roster slots
* selectable #0001
* locked placeholders for unavailable roster slots
* slot numbering within the roster cards unless separately requested otherwise
* fighter portrait
* large fighter preview canvas
* animated fighter preview
* Available status badge for #0001
* Locked status behavior for locked slots
* #0001 fighter name
* Confirm #0001 primary button
* keyboard navigation
* gamepad navigation
* pointer/touch interaction
* first-click select / second-click confirm behavior
* immediate keyboard/gamepad confirmation behavior
* loading of character sprites
* selection state passed into Select Map

Do not change the roster cards themselves just because they contain slot numbers. The requested Roster slot 01 removal refers to the metadata beneath the fighter name in the preview, not necessarily the existing numbers shown on roster tiles.

Preview behavior

With the Idle/Run buttons gone, simplify the preview animation implementation without changing the rendering system.

The preview should continue to animate smoothly using the existing normalized sprite data, preferably using the fighter’s idle animation.

Preserve:

* fitCanvas(...)
* drawFrameAt(...)
* imageSmoothingEnabled = false
* bottom-center positioning
* consistent normalized scale
* existing sprite loading behavior

Do not hardcode a new sprite or replace the existing artwork.

Do not remove the run animation from the character database merely because it is no longer selectable from this menu. Run is still required by gameplay.

CSS cleanup in styles.css

Clean up the CSS associated specifically with the elements removed from Character Select.

The relevant existing rules include approximately:

* .preview-floor
* .is-locked-preview .preview-floor
* .preview-sub
* .preview-anims
* .anim-chips
* .detail-list--compact
* Character Select usages of .chip
* responsive rules referencing .preview-sub
* responsive rules referencing .detail-list--compact
* responsive rules referencing .preview-anims .label

Delete obsolete Character Select-only rules where they are no longer referenced.

However, do not blindly delete generic/shared selectors such as:

* .panel-meta
* .detail-list
* .label
* .chip
* .hint-bar

unless you verify that they are unused everywhere else in the project.

The goal is to clean up dead Character Select styling without causing regressions on another screen.

Preview spacing

The current .preview-info uses a grid with a relatively large gap because it accommodates several blocks:

* heading/status
* animations
* facts
* Confirm button

After those sections are removed, adjust the layout naturally so that the remaining preview information does not have awkward blank vertical space.

The remaining information area should essentially contain:

* availability/status
* fighter name
* optional locked-state explanation only when relevant
* Confirm button

Keep the existing dark Alva visual language, borders, typography, panel geometry, and responsive behavior.

Do not perform a broader redesign.

Do not globally remove menu hints

Although the Select Fighter control hint footer must disappear, hintBar() in js/ui/components.js is a shared UI component.

Do not remove keyboard hints from unrelated screens.

Only stop rendering the hint bar on Character Select.

Update ALVA_SPEC.md

The current Select Fighter specification says the preview contains:

* animated idle/run preview
* availability badge
* animation chips
* frame facts
* Confirm button

Update section 6.4 Select Fighter so the specification matches the new UI.

It should describe the preview as a clean animated fighter preview with the availability state/name and Confirm action, without:

* animation chips
* frame-count facts
* attack-set metadata
* roster-slot metadata
* a preview floor line
* bottom keyboard/control hints
* roster availability-count metadata

Do not alter unrelated design or gameplay requirements in the spec.

Non-goals

Do not:

* change the fighter artwork
* modify #0001’s idle or run frame files
* change the 4 idle / 6 run frame data
* remove the run animation from gameplay
* alter physics
* alter combat
* alter the map-select screen
* alter the battle screen
* change the Home screen
* change the Select Mode screen
* change the 48-slot roster structure
* invent fighter names
* add new UI in place of the removed metadata
* replace removed information with icons/tooltips
* hide elements with hacks when they can cleanly be removed from the DOM
* globally disable keyboard/gamepad navigation
* globally remove the shared menu hint component

Acceptance criteria

After the change, navigate:

Home → Play → Quick Battle → Select Fighter

and verify all of the following.

The Select Fighter screen must not contain:

* 1 / 48 available
* Roster slot 01
* Animations
* an Idle animation chip
* a Run animation chip
* Idle frames
* 4 as an idle-frame fact
* Run frames
* 6 as a run-frame fact
* Attack set
* Awaiting sprites
* the thin floor line underneath the preview fighter
* the bottom Navigate / Select / Back control hint footer

The screen must still contain and correctly operate:

* Select Fighter heading
* Back button
* setup steps
* Roster heading
* 48 roster slots
* #0001 selectable roster card
* locked roster slots
* fighter preview
* animated idle sprite
* Available badge
* #0001 name
* Confirm #0001
* correct focus behavior
* keyboard/gamepad interaction
* pointer/touch interaction
* confirmation into Select Map

Also verify:

* no unexplained empty space remains where removed metadata used to be
* no blank focusable elements remain
* Tab/menu navigation does not land on deleted animation controls
* no console errors are introduced
* no undefined references remain after deleting DOM fields
* short-height responsive layouts still work
* tall/narrow layouts still work
* Select Fighter fits the viewport as before
* other screens retain their own control hints and behavior
* the game still loads through relative paths suitable for GitHub Pages

Keep the patch minimal, intentional, and restricted to this Select Fighter UI cleanup.
