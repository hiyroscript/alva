You are modifying the current Alva repository:

hiyroscript/alva

This is a complete replacement of the current Knockback/launch architecture.

Do not patch the current system.

Do not preserve the current:

* Low / Mid / High Knockback tiers;
* numeric base knockback vectors;
* accumulated-Knockback bonus system;
* knockback growth;
* additive launch formula;
* old resolveLaunch() behavior;
* baseKnockback;
* accumulatedKnockbackAxis;
* knockbackGrowth;
* Knockback-specific scaling helpers.

Replace the entire gameplay launch model with the new system described below.

The new system has only two main concepts:

1. Launch point — a number owned by the fighter.
2. Base launch — an integer 0, 1, 2, or 3 owned by each hit/attack.

The final launch magnitude is:

Launch point × Base launch

This must become the single source of truth for how hard a fighter is launched after a hit.

⸻

1. Core terminology

From this point forward:

Do not call the fighter’s accumulated number:

Knockback

Call it:

Launch point

Player-facing capitalization:

Launch point

Internal recommended property:

launchPoint

Examples:

* 0
* 5
* 25
* 120
* 245

Every fighter starts at:

0 Launch point

⸻

2. What Launch point means

Launch point is the fighter’s accumulated damage-like number.

Every damaging hit adds its damage amount directly to the target’s Launch point.

Example:

fighter begins:

0

BA1 hits for 5:

5

BA2 hits for 10:

15

shuriken hits for 1:

16

another BA1:

21

There is no percentage sign.

There is no Health.

Launch point is not reduced by attacks.

It accumulates until the fighter is eliminated and respawns.

⸻

3. Launch point reset

Launch point starts at:

0

on:

* fresh match;
* fighter respawn;
* Practice fighter reset;
* Practice CPU respawn;
* character swap.

It does not reset simply because:

* an attack ends;
* the fighter touches ground;
* time passes;
* stamina refills.

⸻

4. Damage and Launch point

Damage now has one direct meaning:

Damage = how many Launch points this hit adds.

Examples:

damage: 5

means:

target.launchPoint += 5

damage: 10

means:

target.launchPoint += 10

Keep the existing damage field if convenient.

But comments/docs should explain that damage adds to Launch point.

⸻

5. Base launch

Every attack/hit also has:

baseLaunch

Base launch must be exactly one of:

* 0
* 1
* 2
* 3

No decimals.

No values above 3.

No Low/Mid/High names.

No vector strength values like:

* 140;
* 220;
* 720;
* 800.	

Those belong to the old system and should be removed from launch-strength calculation.

⸻

6. Meaning of each Base launch value

Base launch 0

No launch.

Formula:

Launch point × 0 = 0

Examples:

Launch point 5:
5 × 0 = 0

Launch point 120:
120 × 0 = 0

Launch point 500:
500 × 0 = 0

The target can still:

* take damage;
* gain Launch point;
* receive hitstun/hitstop;

but receives no launch velocity from the hit.

⸻

Base launch 1

Normal launch.

Formula:

Launch point × 1

Example:

Launch point 120:

120 × 1 = 120

Launch strength:

120

⸻

Base launch 2

Double launch.

Formula:

Launch point × 2

Example:

Launch point 120:

120 × 2 = 240

⸻

Base launch 3

Triple launch.

Formula:

Launch point × 3

Example:

Launch point 120:

120 × 3 = 360

This is the strongest allowed Base launch tier.

⸻

7. Important formula

The launch formula is simply:

launchStrength = target.launchPoint * hit.baseLaunch

Use the target’s Launch point after this hit’s damage has been added.

Example:

Target before hit:

115

BA1:

* damage 5;
* Base launch 1.

First add damage:

115 + 5 = 120

Then calculate launch:

120 × 1 = 120

Final launch strength:

120

⸻

8. BA2 example

Target before hit:

110

BA2:

* damage 10;
* Base launch 2.

After damage:

120

Launch:

120 × 2 = 240

Final launch strength:

240

⸻

9. Charged BA2 explosion example

Target before explosion:

105

Explosion:

* damage 15;
* Base launch 3.

After damage:

120

Launch:

120 × 3 = 360

Final launch strength:

360

⸻

10. Zero-launch example

Target:

119

Shuriken:

* damage 1;
* Base launch 0.

After hit:

120

Launch:

120 × 0 = 0

Target now has:

120 Launch point

but is not physically launched by the shuriken.

This is intentional.

⸻

11. Remove the current Knockback architecture completely

The current repository contains an earlier launch system involving:

* js/data/knockback.js;
* KNOCKBACK_LEVELS;
* Low/Mid/High;
* resolveKnockback();
* baseKnockback;
* accumulatedKnockbackAxis;
* knockbackGrowth;
* accumulated bonus;
* horizontal/vertical per-point tuning;
* resolveLaunch();
* related README/spec descriptions.

These systems should no longer determine attack launch strength.

Delete/refactor them rather than keeping them dormant alongside the new design.

There should not be two launch systems.

⸻

12. Replace CombatState.knockback

Current fighter state uses:

combat.knockback

Replace it with:

combat.launchPoint

Recommended:

this.launchPoint = 0

Do not keep combat.knockback as a hidden compatibility mirror unless absolutely required during one migration step.

Final code should use one canonical property:

launchPoint

⸻

13. HUD rename

The top fighter card currently displays the accumulated Knockback number.

Rename the player-facing label/ARIA meaning to:

Launch point

The visual can remain just the number beneath the character name.

Example:

#0001
85

But accessibility should say:

Launch point 85

Do not show:

* Knockback;
* %;
* Health.

⸻

14. Practice damage numbers

Practice floating hit numbers can remain:

+5
+10
+1
+15

because they represent how much Launch point was added.

Update comments from:

“Knockback added”

to:

“Launch point added”

⸻

15. Exact #0001 damage values

Preserve exactly:

BA1

damage:
5

Midair BA1

damage:
5

BA2

damage:
10

Midair BA2

damage:
10

Shuriken

damage:
1

Charged BA2 tick

damage:
1

every:

0.5 seconds

Charged BA2 explosion

damage:
15

Do not change these numbers.

⸻

16. Exact #0001 Base launch values

Implement exactly:

Move	Damage	Base launch
BA1	5	1
Midair BA1	5	2
BA2	10	2
Midair BA2	10	2
Shuriken	1	0
Charged BA2 tick	1	0
Charged BA2 explosion	15	3

Charged BA2’s initial Sphere Rush contact should remain:

* damage 0;
* Base launch 0;

unless current technique flow requires another explicit setup value.

It should bind/connect without prematurely launching.

⸻

17. Charged BA1 clone

Charged BA1 uses the existing clone that performs normal attack definitions.

Therefore:

Ground clone BA1

inherits:

* damage 5;
* Base launch 1.

No-ground fallback midair BA2

inherits:

* damage 10;
* Base launch 2.

Do not duplicate separate clone launch values.

The clone should reuse the actual attack definition.

⸻

18. Launch direction remains separate

Base launch controls:

how strong

the launch is.

It does not describe direction.

Keep launch direction as a separate attack property.

The current game already needs distinct directions:

* horizontal;
* upward;
* downward;
* primarily horizontal Sphere Rush explosion.

Preserve that identity.

Recommended new attack schema:

baseLaunch: 1

plus something like:

launchDirection: 'horizontal'

or:

launch: {
  base: 1,
  direction: 'horizontal'
}

Either is acceptable.

Do not encode direction using huge numeric vectors.

⸻

19. Direction types

The minimum direction set should support:

* horizontal;
* upward;
* downward.

For example:

launchDirection: 'horizontal'

launchDirection: 'up'

launchDirection: 'down'

Sphere Rush explosion should be:

horizontal

because it is intended as a strong horizontal finisher.

⸻

20. #0001 launch directions

Preserve the current move identities:

BA1

Base launch:
1

Direction:
horizontal

Push away from attacker’s facing.

Midair BA1

Base launch:
2

Direction:
up

BA2

Base launch:
2

Direction:
up

Midair BA2

Base launch:
2

Direction:
down

Shuriken

Base launch:
0

Direction can be:

* none;
* horizontal;

but Base launch 0 means final launch is always zero.

Prefer:

launchDirection: null

for zero-launch hits.

Charged BA2 tick

Base launch:
0

Direction:
none.

Charged BA2 explosion

Base launch:
3

Direction:
horizontal

⸻

21. Final velocity conversion

launchStrength = Launch point × Base launch

produces one logical launch strength value.

Convert this into body velocity according to launch direction.

For horizontal:

body.vx = launchStrength * facing

For upward:

body.vy = -launchStrength

For downward:

body.vy = launchStrength

However, inspect current physics scale before finalizing direct world-units usage.

The important gameplay invariant is:

* Launch point 120 × Base launch 1 = logical launch 120;
* ×2 = logical launch 240;
* ×3 = logical launch 360.

If a global physics conversion constant is needed because raw 120 world units/sec is too weak relative to gravity, centralize one universal conversion factor.

For example:

velocity = logicalLaunch × LAUNCH_VELOCITY_SCALE

But:

do not use different multipliers per attack.

The Base launch tier is already the attack-strength multiplier.

Any conversion between logical launch units and world velocity must be global and uniform.

⸻

22. Recommended global velocity scale

Because Alva currently uses relatively large physics values such as gravity around thousands of world units per second squared, raw values like:

5
20
120

may be too small as direct velocities.

Therefore create one global conversion factor if needed.

Example starting point:

LAUNCH_VELOCITY_SCALE = 4

Then:

Launch point 120 × Base 1:

logical:
120

world velocity:
480

Base 2:

logical:
240

world velocity:
960

Base 3:

logical:
360

world velocity:
1440

This preserves the exact 1×/2×/3× relationship.

Tune the single global scale based on gameplay.

Do not tune attack-specific launch speeds outside Base launch.

⸻

23. Critical mathematical invariant

For any target Launch point L:

Base 0:
0 × L = 0

Base 1:
1 × L = L

Base 2:
2 × L = 2L

Base 3:
3 × L = 3L

This relationship must remain exact before the optional universal physics conversion factor.

No additive constant.

No attack-specific growth.

No Low/Mid/High speed lookup.

No exponential formula.

No percentage multiplier.

⸻

24. Launch at zero Launch point

Important consequence:

If a fighter’s Launch point is:

0

and receives a hit with:

damage > 0

the hit’s damage is added first.

So a damaging hit normally still launches.

Example:

fighter at 0.

BA1:
damage 5.

After damage:
5

Launch:
5 × 1 = 5

not zero.

BA2:
damage 10.

After damage:
10

Launch:
10 × 2 = 20

This is why damage must be added before launch calculation.

⸻

25. Zero-damage launching hits

If a future hit has:

* damage 0;
* Base launch > 0;

against a fighter with existing Launch point, it can still launch them.

Example:

target Launch point 100.

Move:

* damage 0;
* Base launch 2.

Launch:
100 × 2 = 200

This is valid.

Therefore do not make launch conditional on damage > 0.

Launch depends on:

* current Launch point after damage;
* Base launch.

⸻

26. Base launch 0 always wins

If:

baseLaunch === 0

final launch strength must always be:

0

regardless of:

* Launch point;
* damage;
* direction;
* attack type.

This should be an explicit fast path.

Example:

if (baseLaunch === 0) finalLaunch = 0

This prevents shuriken/tick regressions.

⸻

27. Validate Base launch values

Create a resolver/validator.

Allowed values:

0
1
2
3

Reject:

* -1
* 4
* 1.5
* '2'
* null unless deliberately treated as 0;
* arbitrary values.

Recommended default:

0

for hits that declare no Base launch.

Malformed definitions should warn and safely become 0.

⸻

28. Remove Low/Mid/High

Delete the current gameplay concept:

KNOCKBACK_LEVELS

Do not preserve:

* Low;
* Mid;
* High;
* horizontal 140/180/220;
* vertical 480/640/800.

Base launch 0–3 replaces attack-strength tiers.

If Discover/UI currently exposes Low/Mid/High Knockback, remove or replace that content.

Do not leave stale documentation teaching a system that no longer exists.

⸻

29. Remove knockback growth

Delete:

knockbackGrowth

entirely.

No attack should have:

* 0.4 growth;
* 1 growth;
* 2 growth;
* any custom per-point growth.

Base launch is the complete attack launch-strength multiplier.

Sphere Rush explosion is stronger because:

baseLaunch: 3

not because of custom growth.

⸻

30. Remove numeric baseKnockback

Delete numeric vectors such as:

baseKnockback: { x: 720, y: 180 }

from gameplay definitions.

Attack launch magnitude must no longer come from numeric velocity vectors.

Instead:

baseLaunch: 3

launchDirection: 'horizontal'

For ordinary attacks:

baseLaunch: 1/2

plus their direction.

⸻

31. Remove accumulated launch bonus

Delete helpers/concepts like:

* accumulated Knockback bonus;
* per-point horizontal bonus;
* per-point vertical bonus;
* default + bonus;
* base + accumulated formula.

The new formula is multiplicative in a much simpler sense:

Base launch tier × Launch point

Do not carry forward the previous additive model.

⸻

32. Remove old Knockback module or repurpose it

js/data/knockback.js currently exists entirely around the old architecture.

Preferred:

rename it to something like:

js/data/launch.js

and rewrite it around the new model.

Possible exports:

BASE_LAUNCH_VALUES

resolveBaseLaunch()

resolveLaunchDirection()

launchStrength()

launchVelocity()

Example:

export const BASE_LAUNCH_VALUES = [0, 1, 2, 3];

export function launchStrength(launchPoint, baseLaunch) {
  return Math.max(0, launchPoint) * baseLaunch;
}

Avoid retaining obsolete Knockback names merely for convenience.

⸻

33. Recommended launch module

Conceptually:

js/data/launch.js

should own:

* valid Base launch tiers;
* direction validation;
* logical launch calculation;
* optional global world-velocity scale.

Example responsibilities:

resolveBaseLaunch(value)

resolveLaunchDirection(value)

calculateLaunchStrength(launchPoint, baseLaunch)

launchVector(strength, direction, facing)

Keep it simple.

⸻

34. Attack definition schema

Update createAttackDefinition().

Old shape:

knockback: { axis, level }

knockbackGrowth

etc.

New shape:

baseLaunch

launchDirection

Example:

ba1: {
  damage: 5,
  baseLaunch: 1,
  launchDirection: 'horizontal'
}

BA2:

baseLaunch: 2

launchDirection: 'up'

⸻

35. Hit definition schema

Projectiles and charged-technique hits should use exactly the same fields.

Example shuriken:

damage: 1,
baseLaunch: 0,
launchDirection: null

Tick:

damage: 1,
baseLaunch: 0,
launchDirection: null

Explosion:

damage: 15,
baseLaunch: 3,
launchDirection: 'horizontal'

Avoid separate bespoke schemas.

⸻

36. CombatState rename

Replace comments/state:

Accumulated Knockback

with:

Launch point

Recommended:

this.launchPoint = 0

No maximum.

No percentage.

It grows by hit damage.

It does not itself eliminate the fighter.

Void remains the elimination mechanism.

⸻

37. applyHit() new flow

Rewrite CombatSystem.applyHit() around this exact sequence:

1. determine whether target blocked;
2. determine actual damage/chip damage;
3. record launchPointBefore;
4. add damage to target Launch point;
5. record launchPointAfter;
6. read hit baseLaunch;
7. calculate:
    launchStrength = launchPointAfter * baseLaunch;
8. resolve direction;
9. convert logical launch strength to final velocity;
10. apply velocity;
11. apply hitstun/hitstop;
12. emit event.

Do not call any old Knockback growth/base-vector functions.

⸻

38. Hit event structure

Replace old event fields such as:

* knockbackBefore
* knockbackAfter
* baseLaunch vector;
* bonusLaunch;
* finalLaunch vector based on old architecture.

Recommended event:

{
  type,
  attacker,
  target,
  move,
  damage,
  launchPointBefore,
  launchPointAfter,
  baseLaunch,
  launchStrength,
  launchDirection,
  projectile,
  summon,
  technique
}

Optionally include applied velocity for debugging.

⸻

39. Block behavior

Do not let the rework accidentally destroy Block.

Current block behavior uses chip damage and reduced launch.

Preserve the high-level concept:

* blocked hit adds chip damage rather than full damage;
* block can reduce/cancel launch according to current design.

But calculate Launch point first using chip damage.

Then:

launchStrength = newLaunchPoint × baseLaunch

Then apply block reduction.

If current block behavior halves horizontal launch and removes vertical launch, preserve that unless another requirement says otherwise.

Do not create separate Base launch values for blocked attacks.

⸻

40. Dodge behavior

Dodge remains simple:

if attack does not connect:

* no damage;
* no Launch point increase;
* no launch.

No special changes.

⸻

41. #0001 BA1 data

Change BA1 to:

* damage 5;
* Base launch 1;
* horizontal launch.

Remove:

* Low Knockback descriptor;
* knockback growth;
* numeric base vector.

Example:

damage: 5
baseLaunch: 1
launchDirection: 'horizontal'

⸻

42. #0001 midair BA1

Change to:

* damage 5;
* Base launch 2;
* upward launch.

Example:

damage: 5
baseLaunch: 2
launchDirection: 'up'

⸻

43. #0001 BA2

Change to:

* damage 10;
* Base launch 2;
* upward launch.

Example:

damage: 10
baseLaunch: 2
launchDirection: 'up'

⸻

44. #0001 midair BA2

Change to:

* damage 10;
* Base launch 2;
* downward launch.

Example:

damage: 10
baseLaunch: 2
launchDirection: 'down'

⸻

45. #0001 shuriken

Change to:

* damage 1;
* Base launch 0;
* no launch direction needed.

A shuriken must increase Launch point but never move the target.

Even at:

Launch point 500

shuriken hit:

after damage:
501

launch:

501 × 0 = 0

No physical push.

⸻

46. Charged BA2 initial contact

Current contact is a setup/bind.

Keep:

* damage 0;
* Base launch 0.

Therefore:

no launch.

It binds the target for the technique.

⸻

47. Charged BA2 ticks

Every 0.5 seconds before explosion:

* damage 1;
* Base launch 0.

Example target begins technique at Launch point 50.

Tick 1:
51
launch 0.

Tick 2:
52
launch 0.

Tick 3:
53
launch 0.

No movement from the ticks.

⸻

48. Charged BA2 explosion

Explosion:

* damage 15;
* Base launch 3;
* horizontal direction.

If target before explosion is:

53

after damage:

68

launch:

68 × 3 = 204

Then apply global physics conversion factor if used.

Do not use:

* 720;
* 180;
* growth 2;
* accumulated horizontal bonus.

Those are obsolete.

⸻

49. Charged BA2 total sequence example

Suppose target begins confirmed Sphere Rush at:

100 Launch point

Ticks at:

0.5 sec:
101

1.0 sec:
102

1.5 sec:
103

Explosion:
+15

new Launch point:

118

Explosion Base launch:

3

Logical launch:

118 × 3 = 354

This is the intended calculation.

⸻

50. Clone behavior

Clone hits use the same attack definitions.

Ground clone BA1:

if target becomes 80 after damage:

Base launch 1

launch:
80

Midair fallback BA2:

if target becomes 80:

Base launch 2

launch:
160

No clone-specific launch formula.

⸻

51. Direction and facing

Horizontal launch should travel away in the hit’s facing direction.

Use the attacker/projectile/clone/technique facing that already exists in the hit resolution path.

Example:

Base launch 2.

Launch point 100.

Strength:

200

Attack facing right:

positive horizontal velocity.

Facing left:

negative horizontal velocity.

⸻

52. Vertical launch

Up:

negative body Y velocity.

Down:

positive body Y velocity.

Keep the game’s existing coordinate convention.

Do not change gravity.

⸻

53. Grounded upward hit

A hit with upward direction and Base launch >0 should:

* set upward velocity;
* mark body ungrounded if appropriate.

Preserve current launch behavior.

⸻

54. Downward hit

A downward hit:

* pushes airborne target downward;
* against grounded target it may immediately collide with the floor.

Preserve current collision physics.

Do not add floor bounce.

⸻

55. Launch point is not velocity

Do not directly store Launch point in:

body.vx

or:

body.vy

without the centralized launch conversion.

Launch point is gameplay state.

Velocity is physics state.

Use:

logical launch strength

then convert to world velocity.

This keeps balancing possible without changing displayed Launch point values.

⸻

56. One global launch scale only

If physics needs amplification, use one global constant.

Example:

LAUNCH_WORLD_SCALE = 4

Then:

logical 120 becomes velocity 480.

logical 240 becomes 960.

logical 360 becomes 1440.

This constant applies to every move equally.

Do not create:

* BA1 launch speed;
* BA2 launch speed;
* Sphere Rush custom numeric speed.

Those differences come exclusively from Base launch.

⸻

57. No minimum launch floor unless necessary

Do not add arbitrary logic like:

Math.max(100, launchStrength)

because that destroys the exact formula at low Launch point.

At low Launch point, launch should genuinely be weak.

Example:

first BA1:

after damage = 5

Base 1:

logical launch = 5.

If that feels too weak in physics, adjust the universal world scale.

Do not add an attack-specific minimum.

⸻

58. No extra Base launch offset

Do not calculate:

(baseLaunch + 1) × launchPoint

Base launch 0 must equal no launch.

Exact formula:

baseLaunch × launchPoint

⸻

59. No additive old launch

Do not do:

oldBaseVelocity + launchPoint × baseLaunch

That would retain the old system.

There is no old base velocity anymore.

The Base launch integer itself is the move’s strength.

⸻

60. No Knockback growth

Do not do:

launchPoint × baseLaunch × growth

There is no growth stat.

Exact formula stays:

launchPoint × baseLaunch

⸻

61. No Low/Mid/High conversion

Do not map:

Base launch 1 → Low 140.

Base launch 2 → Mid 180.

Base launch 3 → High 220.

That would be preserving the previous architecture under new names.

Base launch is literally the multiplier number:

0, 1, 2, or 3.

⸻

62. No attack-specific velocity vectors

Do not preserve:

{ x: 140, y: 0 }

{ x: 0, y: 800 }

{ x: 720, y: 180 }

as attack launch strengths.

Only direction should remain.

Magnitude comes from:

Launch point × Base launch.

⸻

63. Practice HUD

Rename accumulated display from:

Knockback

to:

Launch point

for both:

* P1;
* CPU.

Practice CPU continues to accumulate Launch point normally.

⸻

64. Quick Battle HUD

Both top cards display:

Launch point numeric value.

Examples:

P1:
#0001
63

CPU:
#0001
121

No %.

No Knockback ARIA label.

Use:

Launch point

⸻

65. Time-up logic

Current Quick Battle may use accumulated Knockback as a tiebreaker.

Rename/update this to Launch point.

When score is tied at time-up:

fighter with lower Launch point wins.

If Launch point also equal:
draw.

Do not refer to Knockback in result reason/comments.

⸻

66. Respawn

On the 2-second respawn:

reset:

launchPoint = 0

along with existing:

* stamina;
* exhaustion;
* charged cooldowns;
* transient combat state.

Score persists.

⸻

67. Practice respawn

Practice Void respawn after 2 seconds:

Launch point returns to:

0

No Practice scoring.

⸻

68. Remove old Discover Knockback content

Search UI/data for:

* Knockback Power;
* Knockback levels;
* Low/Mid/High;
* Knockback direction summaries.

If Discover currently documents the old launch system, replace it.

New informational concept:

Launch point

“Damage received increases Launch point. Higher Launch point makes attacks launch the fighter harder.”

Base launch

“Each attack has Base launch 0–3. Final launch strength equals Launch point multiplied by Base launch.”

Do not expose internal physics velocity scale.

⸻

69. Base launch descriptions

If Discover needs readable descriptions:

0 — None

“No launch.”

1 — Normal

“Launches at 1× Launch point.”

2 — Strong

“Launches at 2× Launch point.”

3 — Very Strong

“Launches at 3× Launch point.”

Names are optional.

The numeric values are authoritative.

⸻

70. Update README

Remove all stale language about:

* accumulated Knockback;
* base Knockback;
* Knockback growth;
* Low/Mid/High;
* per-point horizontal/vertical bonus;
* resolveKnockback;
* resolveLaunch;
* additive base + bonus.

Replace with the new simple model.

Recommended explanation:

“Each fighter has a Launch point starting at 0. Damage adds directly to Launch point. Each hit has a Base launch value from 0 to 3. After damage is applied, launch strength is calculated as Launch point × Base launch. Base launch 0 means no launch; 1 uses the Launch point directly; 2 doubles it; 3 triples it. Direction is stored separately from strength.”

⸻

71. Update ALVA_SPEC

Do the same full replacement.

Do not leave historical descriptions of the previous system in the active specification.

⸻

72. Search all comments

Search repository-wide for:

* Knockback;
* knockback;
* baseKnockback;
* knockbackGrowth;
* accumulatedKnockback;
* Low;
* Mid;
* High;
* resolveLaunch;
* resolveKnockback.

Decide case by case.

Keep the word only where it genuinely refers to something unrelated or historical.

Gameplay launch comments should move to Launch point/Base launch terminology.

⸻

73. Tests: Launch point state

Add tests:

fresh fighter:

launchPoint === 0

Damage:

* +5 => 5;
* +10 => 15;
* +1 => 16.

No maximum.

High values remain valid.

⸻

74. Tests: Base launch validation

Verify:

valid:

* 0;
* 1;
* 2;
* 3.	

Invalid:

* -1;
* 4;
* 2.5;
* string values;
* NaN;
* Infinity.

Invalid values should safely resolve to 0 and warn.

⸻

75. Tests: Base 0

For Launch points:

* 1;
* 10;
* 120;
* 500;
* 10,000.

Base launch 0 always yields:

0.

⸻

76. Tests: Base 1

For Launch point:

120

Base 1:

120.

For:
40

Base 1:

40.

⸻

77. Tests: Base 2

Launch point:

120

Base 2:

240.

Launch point:
40

Base 2:

80.

⸻

78. Tests: Base 3

Launch point:

120

Base 3:

360.

Launch point:
40

Base 3:

120.

⸻

79. Tests: exact formula

Core helper test:

for many Launch point values L:

calculateLaunchStrength(L, 0) === 0

calculateLaunchStrength(L, 1) === L

calculateLaunchStrength(L, 2) === L * 2

calculateLaunchStrength(L, 3) === L * 3

This should be one of the most important tests in the project.

⸻

80. Tests: damage happens first

Target:

115

Hit:

* damage 5;
* Base 1.

Expected:

Launch point:
120

launch strength:
120.

Not:
115.

⸻

81. Tests: BA1

Fresh target:

BA1.

Damage:
5.

New Launch point:
5.

Base:
1.

Logical launch:
5.

Second BA1:

new Launch point:
10.

logical launch:
10.

The exact world velocity may be:

logical × global scale.

⸻

82. Tests: BA2

Fresh target:

BA2.

Damage:
10.

Launch point:
10.

Base:
2.

Logical launch:
20.

Later at 110 before hit:

damage:
+10

new:
120.

Base:
2.

logical:
240.

⸻

83. Tests: midair BA1

Damage:
5.

Base:
2.

Direction:
up.

At Launch point after damage 50:

logical launch:
100 upward.

⸻

84. Tests: midair BA2

Damage:
10.

Base:
2.

Direction:
down.

At new Launch point 120:

logical launch:
240 downward.

⸻

85. Tests: shuriken

Fresh target:

shuriken:

* damage 1;
* Launch point becomes 1;
* Base 0;
* zero launch.

At target 119:

shuriken:

* new Launch point 120;
* zero launch.

At 999:

* becomes 1000;
* zero launch.

⸻

86. Tests: charged BA2 ticks

Start target:

Each tick:

101;
102;
103;

Each tick:

Base 0.

No launch velocity.

⸻

87. Tests: charged BA2 explosion

After ticks, suppose:

Explosion:
+15.

New:

Base 3.

Logical launch:

354.

Horizontal direction.

Verify exact result before global velocity scaling.

⸻

88. Tests: clone BA1

Clone ground attack:

damage 5.

Base 1.

Uses same formula as real BA1.

No clone-specific launch strength.

⸻

89. Tests: clone fallback

Clone fallback midair BA2:

damage 10.

Base 2.

Direction down.

Uses same attack data as regular midair BA2.

⸻

90. Tests: direction does not alter strength

For same:

Launch point = 100.

Base = 2.

All directions must have logical magnitude:

200.

Horizontal:
200 sideways.

Up:
200 upward.

Down:
200 downward.

Direction changes vector orientation, not strength.

⸻

91. Tests: global scale

If using:

LAUNCH_WORLD_SCALE

test:

appliedSpeed = logicalLaunch × scale

The same scale must apply to every direction and every attack.

No attack-specific exceptions.

⸻

92. Tests: HUD rename

Verify:

* no Knockback label;
* Launch point displayed;
* number updates after damage;
* resets on respawn;
* no percentage.

⸻

93. Tests: time-up

Score tied.

P1 Launch point:
50.

P2:
80.

P1 wins time tiebreak.

If both:
50.

draw.

⸻

94. Tests to remove

Delete/update tests asserting:

* Low/Mid/High values;
* 140/180/220 horizontal;
* 480/640/800 vertical;
* baseKnockback;
* accumulated bonus;
* knockback growth;
* Sphere Rush numeric 720 launch;
* additive launch;
* resolveKnockback;
* old resolveLaunch;
* Knockback HUD terminology.

Do not keep obsolete tests disabled.

Rewrite them around Launch point.

⸻

95. File architecture suggestion

Recommended changes:

New

js/data/launch.js

Rewrite

js/game/combat.js

Update

js/data/characters.js
js/game/hud.js
js/game/practice.js
js/game/battle.js
js/game/fighter-status.js
README.md
ALVA_SPEC.md

Remove

js/data/knockback.js

if nothing else requires it after migration.

Update all imports.

⸻

96. Preserve current hit reaction systems

Do not remove:

* hitstun;
* hitstop;
* hurt animation;
* blockstun;
* Dodge;
* bind;
* gravity.

Only replace how launch strength is calculated.

⸻

97. Preserve current non-launch systems

Do not alter:

* stages;
* Void;
* 3-point scoring;
* 2-second respawn;
* stamina;
* Dash;
* CAB cooldowns;
* Practice CPU;
* HUD layout;
* portrait orientation;
* manual fighter facing;
* camera;
* attack timing;
* animation;
* hitboxes.

This is specifically a Launch-system rework.

⸻

98. Naming rule

After this migration, use:

Launch point
for the fighter’s accumulated number.

Use:

Base launch
for the attack’s 0–3 multiplier.

Use:

Launch strength
for the computed:

Launch point × Base launch

Use:

Launch direction
for horizontal/up/down.

Avoid calling any of these:

* Knockback;
* Knockback growth;
* base Knockback;
* accumulated Knockback.

⸻

99. Mental model

This should be the complete system:

Fighter

owns:
Launch point

starts:
0

Hit

owns:
damage
Base launch
Launch direction

On hit

Launch point += damage

then:

Launch strength = Launch point × Base launch

then:

apply Launch strength in Launch direction.

That is all.

⸻

100. #0001 final reference table

Hit	Damage	Base launch	Direction
BA1	5	1	Horizontal
Midair BA1	5	2	Up
BA2	10	2	Up
Midair BA2	10	2	Down
Shuriken	1	0	None
Charged BA2 contact	0	0	None
Charged BA2 tick	1	0	None
Charged BA2 explosion	15	3	Horizontal

Charged BA1 clone inherits BA1 or midair BA2 depending on its existing fallback behavior.

⸻

101. Final acceptance criteria

This rework is complete when:

Launch point

* every fighter starts at 0;
* damage adds directly to Launch point;
* HUD displays Launch point;
* no percentage;
* respawn resets to 0.

Base launch

* only values 0,1,2,3 exist;
* each hit has one;
* no Low/Mid/High strength system remains;
* no numeric attack launch vectors remain.

Formula

* final logical launch = current Launch point after damage × Base launch;
* Base 0 always gives 0;
* Base 1 gives 1×;
* Base 2 gives 2×;
* Base 3 gives 3×.

Directions

* BA1 horizontal;
* midair BA1 up;
* BA2 up;
* midair BA2 down;
* shuriken none;
* CAB2 ticks none;
* CAB2 explosion horizontal.

#0001 damage

* BA1 5;
* midair BA1 5;
* BA2 10;
* midair BA2 10;
* shuriken 1;
* CAB2 ticks 1 every 0.5 sec;
* CAB2 explosion 15.

Old system removed

* no KNOCKBACK_LEVELS;
* no Low/Mid/High gameplay tiers;
* no baseKnockback;
* no knockbackGrowth;
* no accumulated bonus;
* no additive launch model;
* no old numeric launch vectors;
* no stale Knockback HUD terminology.

Quality

* tests explicitly verify the formula;
* shuriken/ticks never launch;
* explosion uses Base launch 3;
* clone reuses real move data;
* global physics conversion, if required, is uniform;
* full test suite passes.

⸻

102. Strict scope

Completely replace the current launch/Knockback system with:

Launch point + Base launch.

Do not partially reuse the previous launch model.

Do not create a hybrid.

The final authoritative formula is:

Launch point after damage × Base launch = Launch strength

with:

Base launch ∈ {0, 1, 2, 3}

and Launch direction handled separately.

Everything else in the game should remain unchanged.
