You are working directly in the existing GitHub repository:

hiyroscript/maxy

Your task is to perform a complete product rebrand from Maxy / MAXY to Alva / ALVA, modernize the interface color system, and completely redesign the Home Screen into a clean, minimalist, professional application-style experience.

This is an existing working browser fighting game. Do not rebuild the project from scratch, replace its architecture, or create a second app.

First inspect the repository and understand the existing implementation before making changes.

PRIMARY OBJECTIVES

Complete all three of these goals:

1. Completely rename the product from Maxy to Alva everywhere.
2. Replace the existing black/orange brand identity with a new white-dominant monochrome system using white, black, and gray.
3. Completely redesign the Home Screen so it feels clean, minimalist, refined, and professional—closer to a premium modern app than a traditional loud fighting-game title screen.

The resulting product should unmistakably feel like a new identity called Alva, not a recolored version of Maxy.

⸻

EXISTING PROJECT — UNDERSTAND BEFORE EDITING

The existing game is a dependency-free browser-based 2D sprite fighting game using:

* HTML
* CSS
* vanilla JavaScript ES modules
* HTML5 Canvas 2D
* no framework
* no build step
* relative asset/module URLs for GitHub Pages compatibility

The existing application already has:

* splash screen
* Home Screen
* mode selection
* character selection
* map selection
* battle screen
* pause UI
* help and credits
* loading/error overlays
* keyboard navigation
* mouse input
* touch controls
* gamepad input
* responsive landscape layouts
* reduced-motion support
* sprite normalization
* fixed-step fighting simulation
* camera system
* HUD
* procedural stages

Do not break any of those systems.

Important existing files include:

* index.html
* styles.css
* README.md
* max
* js/config.js
* js/core/app.js
* js/core/screen-manager.js
* js/core/menu-navigator.js
* js/screens/splash-screen.js
* js/screens/home-screen.js
* js/screens/mode-select-screen.js
* js/screens/character-select-screen.js
* js/screens/map-select-screen.js
* js/screens/help-credits-screen.js
* js/screens/battle-screen.js
* js/ui/logo.js
* js/ui/components.js
* js/ui/help-content.js
* js/ui/overlays.js
* js/game/*
* js/data/*
* js/stages/*

The current Home Screen is specifically implemented in:

* js/screens/home-screen.js
* the Home section of styles.css
* js/ui/logo.js

Currently it contains:

* black full-screen background
* grid decoration
* large orange diagonal slash
* animated horizontal streaks
* large fighter silhouette
* orange rim treatment
* custom skewed MAXY geometric SVG wordmark
* numbered 01 PLAY / 02 HELP AND CREDITS menu buttons
* orange primary action
* keyboard hint footer
* developer/build information

Do not preserve that composition simply with different colors.

Redesign it substantially.

⸻

PART 1 — COMPLETE MAXY → ALVA REBRAND

The old product name must disappear from the active project.

Use these naming conventions:

* MAXY → ALVA
* Maxy → Alva
* maxy → alva where it represents the product/repository/deployment slug

Perform a case-insensitive audit of all tracked text files.

Do not limit the rebrand to visible text.

Check:

* page title
* HTML metadata
* descriptions
* accessibility labels
* SVG titles and aria labels
* JavaScript constants
* comments
* console messages
* credits
* help text
* documentation
* README
* deployment instructions
* developer documentation/specification files
* splash/home/menu copy
* error messages
* loading UI
* any internal product-name strings
* GitHub Pages example URLs
* repository-related references
* CSS comments/headings
* favicon branding

Known places that currently contain Maxy branding include at minimum:

index.html

Currently contains:

* <title>MAXY</title>
* MAXY description metadata
* orange MAXY-style favicon
* rotation message referring to Maxy
* boot error copy containing MAXY
* noscript copy containing MAXY

Change all of these to Alva.

js/config.js

Change:

title: 'MAXY'

to:

title: 'ALVA'

Update the file comments from Maxy to Alva as well.

Where practical, use CONFIG.title rather than duplicating the product name in new runtime code.

js/core/app.js

The current asset errors use console prefixes such as:

[Maxy]

Change those to:

[Alva]

Audit any similar diagnostic strings elsewhere.

js/ui/logo.js

This is currently a bespoke geometric MAXY SVG made from four custom polygon-based letters.

It must be replaced with a new ALVA identity.

Do not simply rename the SVG accessibility title while leaving the MAXY shapes.

Create an actual ALVA wordmark.

js/screens/help-credits-screen.js

The current header uses MAXY as a kicker.

Replace it with ALVA or use CONFIG.title.

js/ui/help-content.js

The credits currently contain:

* a MAXY card
* text saying work is original to Maxy

Rewrite all of this for Alva while preserving the existing third-party sprite attribution and rights notices.

Do not remove or weaken existing attribution for the #0001 sprite source.

js/screens/mode-select-screen.js

The explanatory copy currently says Quick Battle is the first mode in Maxy.

Rewrite it to Alva.

README.md

Rewrite all product-level Maxy references to Alva.

Update:

* heading
* project description
* architecture wording
* credits
* branding descriptions
* deployment examples
* GitHub Pages examples

If the repository is eventually renamed to alva, documentation should use the /alva/ GitHub Pages path.

Keep asset paths relative so the project continues to work regardless of GitHub Pages subdirectory.

max

This file contains the original Maxy project specification and includes extensive old branding and the old black/white/gray/orange visual requirements.

Do not leave this as an obsolete Maxy specification.

Either:

* rename it to a clearer Alva-oriented specification file such as ALVA_SPEC.md

or, if there is a reason to retain its existing filename:

* rewrite its contents for Alva

Preferred solution: give the file a meaningful descriptive name rather than keeping the ambiguous max filename.

Its branding requirements must match the new white/black/gray identity.

Repository-level name

If the environment/API has permission to rename the GitHub repository itself, rename:

maxy

to:

alva

If repository renaming is outside the coding environment’s capabilities, make all code and documentation ready for that repository rename without breaking the existing deployment.

Do not introduce hard-coded /alva/ root paths into runtime assets. Continue using relative paths.

⸻

PART 2 — NEW ALVA BRAND IDENTITY

The new identity is deliberately restrained.

Core palette

The Alva application UI must use only a monochrome family:

* White — dominant
* Black
* Gray tones

Recommended semantic palette:

--bg: #ffffff;
--surface: #ffffff;
--surface-subtle: #f7f7f7;
--surface-muted: #f1f1f1;
--text: #111111;
--text-strong: #000000;
--text-secondary: #666666;
--text-muted: #8a8a8a;
--border: #e2e2e2;
--border-strong: #c8c8c8;
--black: #000000;
--white: #ffffff;

Exact neutral values can be refined for contrast and visual quality.

Remove the orange brand system

The current interface defines and heavily uses:

* --orange
* --orange-hi
* --orange-soft
* #ff7a00
* #ff9a2e
* rgba(255, 122, 0, ...)

These must no longer be part of the Alva interface design system.

Perform an audit of styles.css and JavaScript-generated color values.

Replace orange UI states with appropriate monochrome treatments.

Examples:

Primary action:

* black background
* white text

Secondary action:

* white or light-gray background
* black text
* gray border

Selected state:

* black border
* subtle light-gray background
* check icon or filled black indicator

Focus state:

* high-contrast black focus ring
* optionally a secondary white offset ring when necessary on dark surfaces

Progress bars:

* black foreground
* pale gray track

Active tabs:

* black text
* black underline or border

Muted states:

* neutral gray

Do not substitute orange with another accent color.

There should be no blue, purple, green, red, gold, or replacement brand accent.

The identity is intentionally monochrome.

Important gameplay exception

The monochrome rule applies to:

* application chrome
* branding
* menus
* buttons
* cards
* overlays
* decorative backgrounds
* focus/selection states
* Home Screen presentation

Do not automatically recolor the original character sprite artwork or the actual stage artwork just to force gameplay into monochrome.

Character sprites and stage art are game content, not brand chrome.

Preserve gameplay art unless a color is clearly being generated specifically as a UI/brand accent.

For example, orange fighter rim effects used by menus should be removed/reworked, but the original source colors inside a sprite should not be destructively edited.

⸻

PART 3 — LIGHT / WHITE-DOMINANT APPLICATION THEME

The existing UI is predominantly near-black.

Alva should reverse that hierarchy.

White must become the dominant application color.

This does not mean every object needs to be pure white.

Instead:

* page/menu backgrounds should generally be white
* surfaces may use #f7f7f7 or similar soft gray
* primary text should be black
* secondary text should be medium gray
* borders should be subtle light gray
* primary CTA should be black
* major panels should have excellent whitespace
* dark elements should be deliberate and limited

Avoid making the redesign look sterile or unfinished.

The difference between surfaces should come from:

* spacing
* thin borders
* subtle gray backgrounds
* restrained shadows
* typography hierarchy

—not from bright accent colors.

Color scheme metadata

Review index.html.

The project currently declares a dark browser theme.

Update appropriately for the white-first interface:

* theme-color should align with the new white/light UI
* browser color-scheme should no longer globally force dark mode
* mobile browser/status bar configuration should look intentional with a white-dominant interface

The battle screen may still explicitly use dark UI elements where appropriate for readability.

⸻

PART 4 — NEW ALVA WORDMARK

Completely replace the current MAXY-specific polygon wordmark.

The new wordmark must read:

ALVA

It should feel:

* minimal
* precise
* modern
* confident
* app-like
* timeless
* highly legible

Avoid:

* orange accents
* racing stripes
* extreme skew
* flames
* esports styling
* graffiti aesthetics
* gaming clichés
* exaggerated sci-fi styling
* large drop shadows
* faux metallic effects

Preferred direction:

A restrained bold or semibold uppercase wordmark with carefully controlled spacing.

The wordmark can still live in js/ui/logo.js, but simplify the implementation if that creates a cleaner result.

It is acceptable to use a custom inline SVG.

Do not require external font downloads.

Use either:

* clean geometric SVG letterforms

or:

* an SVG/text treatment based on the existing system font stack

The logo must work in:

* the Home Screen
* loading overlay
* any small brand context
* accessibility tree

Make sure:

* SVG <title> says ALVA
* aria-label says ALVA
* all visible letters genuinely form ALVA
* there is no leftover X/Y geometry from MAXY

Create a simple favicon as well.

Preferred favicon direction:

* white background
* black minimal A or ALVA monogram
* no orange
* clear at small sizes

⸻

PART 5 — COMPLETELY REDESIGN THE HOME SCREEN

This is the most important visual change.

Do not lightly modify the existing Home Screen.

Treat it as a new interface.

The current fighting-game splash-menu composition should disappear.

Remove the current:

* orange diagonal slash
* orange rim glow
* moving streaks
* decorative grid-heavy composition
* giant full-bleed fighter silhouette
* skewed MAXY display logo
* numbered 01 / 02 arcade-menu styling
* orange-filled menu button
* loud fighting-game visual language

The new Home Screen should feel like opening a polished modern application.

Think:

* premium desktop/mobile app
* generous white space
* excellent alignment
* calm hierarchy
* restrained animation
* strong typography
* clear actions
* functional rather than decorative

Do not copy Apple, Notion, Linear, Arc, or any other application directly.

Use the general design principles of modern professional software without cloning a specific product.

⸻

HOME SCREEN — REQUIRED STRUCTURE

Create a centered responsive application shell.

Suggested structure:

┌─────────────────────────────────────────────────────────────┐
│ ALVA                                          BUILD 0.1.0   │
│─────────────────────────────────────────────────────────────│
│                                                             │
│              2D SPRITE FIGHTING GAME                        │
│                                                             │
│              ALVA                                           │
│              Clean supporting copy                          │
│                                                             │
│              [ Play ]                                       │
│              [ Help & Credits ]                             │
│                                                             │
│                                  subtle fighter preview      │
│                                                             │
│─────────────────────────────────────────────────────────────│
│ by hiyroscript                           keyboard hints      │
└─────────────────────────────────────────────────────────────┘

This diagram is conceptual, not a pixel-perfect requirement.

The implementation should be polished rather than mechanically matching ASCII art.

Overall background

Use:

#ffffff

or the final chosen equivalent white token.

No large gradients.

No orange or colored ambient lighting.

If subtle visual separation is useful, use:

* #f7f7f7
* very soft gray border
* extremely subtle neutral shadow

Keep the canvas calm.

⸻

HOME HEADER

Create a simple top header inside the application shell.

Left:

* small/medium ALVA wordmark

Right:

* build/version label, e.g. BUILD 0.1.0

Optional:

* subtle text/icon shortcut to Help, only if it improves usability without duplicating the main actions excessively

Use a thin light-gray divider beneath the header if useful.

Do not make this resemble a game HUD.

⸻

HOME MAIN CONTENT

The central content should have a clear hierarchy.

Suggested left/main section:

Small eyebrow:

2D SPRITE FIGHTING GAME

Then the main ALVA wordmark/title.

Under it, use only a small amount of supporting copy.

Possible direction:

Fast, responsive browser fighting.

or another similarly short factual line.

Do not add marketing paragraphs.

The Home Screen should remain minimal.

⸻

HOME ACTIONS

Retain the two existing functional destinations:

1. Play
2. Help & Credits

Do not change navigation behavior.

PLAY must still go to the existing mode-select flow.

HELP & CREDITS must still go to the existing help screen.

Restyle them as professional application controls.

Play button

Primary action:

* black background
* white text
* medium rounded corner
* simple arrow icon if desired
* no orange
* no giant transform animation
* no arcade index number

Hover:

* slightly lighter black/dark gray

Focus:

* clearly visible monochrome focus ring

Pressed:

* subtle scale or darkening feedback

Help & Credits

Secondary action:

* white or very light gray
* black text
* thin gray border

Hover:

* light gray background

Focus:

* strong black outline/ring

Buttons should have generous touch targets.

Keep all existing keyboard/gamepad navigation mechanisms working.

⸻

OPTIONAL FIGHTER PREVIEW

The game currently loads #0001 and renders an animated silhouette on the Home Screen.

You may keep the existing technical ability to preview the fighter, but completely change its presentation.

Preferred direction:

Place a restrained preview on the right side of the Home content in a contained light-gray panel/card.

For example:

* #f6f6f6 background
* thin gray border
* rounded corners
* black or charcoal silhouette
* very subtle gray shadow
* idle animation only
* no glow
* no orange rim
* no fullscreen character
* no overlapping the main navigation

The fighter should be supporting visual content, not the dominant element.

Use existing loaded/normalized sprite data.

Do not duplicate sprite-loading logic.

Do not edit the original #0001 PNGs.

If the fighter preview makes the Home Screen less clean at compact dimensions, hide or reduce it responsively.

⸻

HOME FOOTER

Keep the useful information already present:

* by hiyroscript
* build version
* keyboard navigation hints where applicable

But visually simplify it.

Use:

* small gray text
* thin divider
* low visual priority

Keyboard hints should remain hidden on touch-first devices as the existing application already does.

⸻

HOME SCREEN RESPONSIVENESS

The game supports:

* desktop/laptop
* phone landscape
* tablet landscape
* unusual browser sizes

The Home Screen must adapt cleanly.

Wide desktop

Allow a two-column main area:

* branding/actions on left
* subtle fighter preview on right

Use a centered maximum width rather than spreading content across an ultrawide display.

Suggested content maximum width:

approximately 1100px–1200px

Tablet / compact landscape

Reduce spacing and type scale.

The fighter preview may become smaller.

Buttons must remain comfortably tappable.

Very short phone landscape

Prioritize:

1. ALVA identity
2. Play
3. Help & Credits

Hide nonessential decorative content first.

The interface must not overflow vertically.

Do not force the user to scroll the Home Screen.

Respect safe-area insets.

Portrait

Do not break the existing portrait-block/rotation system.

Change its copy from Maxy to Alva.

⸻

PART 6 — REMOVE GAMEY HOME EFFECTS FROM JAVASCRIPT

js/screens/home-screen.js currently creates:

* .home-bg
* .home-grid
* .home-slash
* .home-streaks
* multiple animated streak spans
* large .home-fighter
* orange fighter rim generated through tintFrame(... '#ff7a00' ...)
* numbered menu markup

Refactor this substantially.

Do not leave unused Home DOM elements or unused CSS after the redesign.

Remove old visual elements instead of hiding them indefinitely.

Simplify the screen class.

If the fighter preview remains:

* keep only the animation/update logic actually required
* use neutral monochrome tinting
* keep imageSmoothingEnabled = false
* retain resize correctness
* respect device pixel ratio
* retain normalized sprite usage

If the new Home Screen no longer requires animation, remove unnecessary rendering/update complexity cleanly.

Do not leave dead code.

⸻

PART 7 — MODERNIZE THE GLOBAL UI DESIGN SYSTEM

Although the Home Screen receives the largest redesign, the rest of the application must no longer look like an unrelated orange-on-black product.

Restyle shared application chrome so Alva feels coherent across screens.

This includes:

* mode select
* fighter select
* map select
* help/credits
* loading overlay
* confirmation dialog
* pause menu
* buttons
* tabs
* cards
* headers
* setup step indicators
* hint bars
* badges
* selection/focus states

Do not necessarily redesign every screen from zero.

Instead, preserve the existing layouts and usability while translating them into the new Alva visual system.

Examples:

Screen backgrounds

Old:

* near black
* orange radial glow
* grid texture

New:

* white
* subtle gray surfaces
* minimal texture or preferably none

Panels

Use:

* white backgrounds
* thin #e2e2e2 borders
* optional light shadow
* restrained radii

Screen headings

Remove or significantly reduce the current aggressive skewed treatment.

Use clean application typography.

Kicker labels

Change orange labels to neutral gray/black.

Setup steps

Completed:

* medium gray

Current:

* black
* black underline/border

Future:

* light gray

Selected cards

Use:

* black border
* subtle gray background
* black checkmark

Not orange.

Primary controls

Black fill / white text.

Information labels

Gray.

Tabs

Active:

* black text
* black underline

Inactive:

* gray text

⸻

PART 8 — LOADING / OVERLAYS

The loading overlay currently uses a dark theme and the MAXY SVG.

Update it to Alva.

Suggested loading experience:

* white or near-white background
* small ALVA wordmark
* black loading label
* light gray progress track
* black progress fill

Error state:

* readable black text
* primary Retry button in black
* secondary Back button in white/light gray

Confirmation dialogs:

* white panel
* subtle gray border/shadow
* black typography
* black primary confirmation button
* neutral secondary cancel button

Retain all existing accessibility behavior.

⸻

PART 9 — SPLASH SCREEN

The existing splash is intentionally understated:

a game by hiyroscript

Keep that concept.

It may remain dark if desired because it functions as a cinematic transition rather than application chrome.

However:

* do not show MAXY anywhere
* transition elegantly from the black splash into the new bright Alva Home Screen
* make the transition feel intentional rather than a sudden broken flash
* retain reduced-motion behavior
* retain skip support

Do not add a noisy logo animation.

⸻

PART 10 — HELP / CREDITS

Preserve all factual attribution.

The existing credits correctly identify the #0001 sprite source.

Keep the third-party attribution and rights statements.

Only change the Maxy-specific ownership/branding wording.

For example, the existing statement describing code/UI/stage art as original to Maxy should now refer to Alva.

Do not imply ownership of:

* Naruto
* Jump Ultimate Stars
* Nintendo IP
* The Spriters Resource material

Do not remove existing attribution merely for visual simplification.

⸻

PART 11 — BATTLE SCREEN

Do not rewrite the fighting engine.

Preserve:

* physics
* state machine
* combat logic
* camera
* input
* touch controls
* timer
* pause functionality
* HUD behavior
* sprite rendering
* stage collision
* stage artwork

Only update the UI chrome to fit Alva where necessary.

Examples:

* HUD accent colors
* pause controls
* overlay panels
* selection states
* result UI
* touch button active-state accents

Replace orange UI accents with monochrome equivalents.

Do not turn the entire stage or character sprites grayscale.

⸻

PART 12 — ACCESSIBILITY AND INTERACTION

Do not regress accessibility during the redesign.

Maintain or improve:

* semantic buttons
* keyboard navigation
* focus indicators
* aria-label
* aria-labelledby
* tab semantics
* dialog semantics
* keyboard/gamepad interaction
* touch targets
* visible focus
* reduced motion
* safe-area support
* contrast

Do not rely solely on color to communicate:

* focus
* selected state
* availability
* errors
* current setup step

Use borders, icons, labels, shape, and weight as appropriate.

⸻

PART 13 — TYPOGRAPHY

Continue using system fonts.

Do not add an external font dependency.

Recommended stack can remain similar to:

system-ui,
-apple-system,
"Segoe UI",
Roboto,
"Helvetica Neue",
Arial,
sans-serif

Use typography to create hierarchy instead of visual effects.

Preferred characteristics:

* strong but not oversized headings
* normal/non-skewed body text
* restrained tracking
* readable small labels
* semibold/bold rather than extreme 900 everywhere
* sentence/title case where it feels more app-like

You may keep uppercase ALVA branding.

Avoid making every control uppercase if title case produces a more polished application feel.

⸻

PART 14 — MOTION

Alva should use subtle motion.

Good:

* 120–220ms hover/focus transitions
* gentle screen transitions
* slight button press feedback
* restrained fighter idle animation
* subtle opacity/translate entrance

Avoid:

* moving light streaks
* dramatic diagonal wipes
* glowing accents
* constant ambient animations
* large hover translations
* excessive scale effects

Respect prefers-reduced-motion.

⸻

PART 15 — CSS CLEANUP

Do not just append a giant override section at the end of styles.css.

Refactor the existing stylesheet.

Remove obsolete Maxy-era Home styles and old orange token usage.

Consolidate the design tokens.

Use meaningful semantic variables.

A preferred approach is something like:

:root {
  --bg: #fff;
  --surface: #fff;
  --surface-subtle: #f7f7f7;
  --surface-muted: #f1f1f1;
  --text: #111;
  --text-strong: #000;
  --text-secondary: #666;
  --text-muted: #8a8a8a;
  --border: #e2e2e2;
  --border-strong: #c8c8c8;
  --black: #000;
  --white: #fff;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 18px;
  --focus-ring: 0 0 0 2px #fff, 0 0 0 4px #000;
}

The exact naming can differ if a better architecture fits the current stylesheet.

The important requirement is a coherent semantic system without the old orange identity.

⸻

PART 16 — HTML METADATA AND FAVICON

Update index.html thoroughly.

Expected branding direction:

<title>ALVA</title>

Description should describe Alva rather than Maxy.

Update:

* theme color
* color scheme
* favicon
* rotate overlay copy
* boot error
* noscript copy

Example copy:

Alva — a 2D sprite fighting game by hiyroscript.

Keep wording concise.

The favicon should be a new monochrome Alva mark.

⸻

PART 17 — DO NOT BREAK EXISTING GAME FLOW

After changes, this flow must still work:

Splash
→ Home
→ Play
→ Select Mode
→ Select Fighter
→ Select Stage
→ Battle
→ Pause / Resume / Restart / Help / Home

Help & Credits must also remain directly accessible from Home.

Preserve selection state and screen-manager behavior.

Do not replace the existing application controller with a new router.

Do not introduce a framework.

Do not add npm/build dependencies unless absolutely unavoidable. There should be no reason to do so for this task.

⸻

PART 18 — TECHNICAL CONSTRAINTS

Maintain:

* working index.html
* ES modules
* relative URLs
* GitHub Pages compatibility
* Canvas 2D battle renderer
* current sprite normalization
* pixel-art rendering
* current assets
* existing game mechanics
* keyboard controls
* gamepad controls
* touch controls
* responsive layouts

Do not:

* convert to React/Vue/Svelte
* add Tailwind
* add Bootstrap
* add WebGL
* add Three.js
* replace the game engine
* recreate sprite assets
* alter source character artwork
* remove credits
* add unnecessary dependencies

⸻

PART 19 — REPOSITORY CLEANUP

After implementation, search the entire working tree for old branding.

There should be no active Maxy references left.

Audit at minimum for:

MAXY
Maxy
maxy
#ff7a00
#FF7A00
#ff9a2e
255, 122, 0
--orange

Handle results intelligently.

For example, old strings should not survive in:

* comments
* runtime text
* metadata
* documentation
* CSS variables
* JS-generated color values
* logo code
* spec documents

If a maxy string exists solely because the Git remote/repository has not yet been renamed and cannot be renamed programmatically, document that specific limitation rather than silently leaving unrelated old branding.

Remove unused CSS selectors associated with:

* .home-grid
* .home-slash
* .home-streaks
* old orange Home treatments

if they are no longer used.

Remove obsolete JS imports/functions if the redesigned Home no longer needs them.

⸻

PART 20 — QUALITY BAR

The result should look intentionally designed.

Do not stop after:

* changing MAXY text to ALVA
* changing orange to gray
* changing background from black to white

That is not sufficient.

The Home Screen must have a genuinely new layout and visual hierarchy.

The redesign should demonstrate:

* professional spacing
* consistent alignment
* strong component hierarchy
* restrained visual effects
* clean responsive behavior
* polished hover/focus/pressed states
* strong monochrome identity
* thoughtful use of the existing fighter artwork
* no visual remnants of the old Maxy theme

The user should immediately perceive Alva as a cleaner and more mature product.

⸻

PART 21 — ACCEPTANCE CHECKLIST

Before considering the work complete, verify all of the following.

Branding

* Product is called ALVA in browser title.
* Product metadata says Alva.
* Home shows ALVA.
* ALVA logo actually depicts ALVA.
* Loading UI uses ALVA.
* Help/Credits says ALVA.
* Console prefixes use Alva.
* README says Alva.
* Original specification/documentation has been updated.
* Rotate message says Alva.
* Error/no-JS messages say ALVA/Alva.
* Old MAXY/Maxy branding is removed.

Color

* White is the dominant application UI color.
* Black is the main high-contrast action/text color.
* Gray is used for hierarchy, surfaces and borders.
* Orange brand variables are removed.
* Orange menu accents are removed.
* No replacement accent color has been introduced.
* Gameplay sprites/stage art have not been destructively recolored.

Home Screen

* Existing diagonal orange slash is gone.
* Animated streaks are gone.
* Heavy grid treatment is gone.
* Old MAXY logo is gone.
* Numbered arcade menu presentation is gone.
* Home is white-dominant.
* Home resembles a polished modern app.
* Play remains the obvious primary action.
* Help & Credits remains accessible.
* Developer/build information is retained subtly.
* Fighter preview, if retained, is contained and monochrome.
* Desktop layout is polished.
* Tablet landscape is polished.
* Phone landscape fits without overflow.
* Keyboard navigation still works.
* Touch controls/navigation still work.
* Reduced motion still works.

Game

* Splash works.
* Play navigation works.
* Mode selection works.
* Character selection works.
* Map selection works.
* Battle launches.
* Pause works.
* Help/Credits works.
* Return-to-home works.
* Character sprites still render correctly.
* Stage rendering still works.
* No console errors were introduced.

⸻

PART 22 — FINAL VERIFICATION

Run the project from a local HTTP server, not from file://.

Test at multiple viewport sizes, including:

* desktop 1920×1080
* laptop 1366×768
* tablet landscape approximately 1024×768
* phone landscape approximately 844×390
* a very short landscape viewport

Verify:

* no clipped Home content
* no accidental scrollbars
* no hidden primary controls
* correct focus order
* sufficient contrast
* correct safe-area handling
* no broken canvas resizing
* no missing assets
* no broken relative URLs

Inspect the browser console.

Fix all errors introduced by the redesign.

Finally perform a repository-wide search for stale Maxy and orange-brand references.

Do not declare the task finished until the rebrand is complete, the new Home Screen is visibly distinct from the old one, and the complete game flow still functions.
