Work in the current `hiyroscript/alva` repository on `main` and fix #0001’s Charged BA2 / Sphere Rush again.

IMPORTANT: inspect the CURRENT implementation first. The previous fix has already been merged. Do not revert it wholesale.

Current main already correctly has:
- a dedicated ChargedTechnique architecture;
- immediate Hurt / Mid-air Hurt on the exact contact tick;
- prasen7-9 rotating on the enemy;
- a whiff release instead of snapping instantly to Idle;
- correct first-hit + delayed-explosion combat structure;
- bind ownership and release-before-knockback;
- fixed facing, grounding rules, wall behavior, interruption handling, etc.

Keep all of those good parts.

The remaining problem is that the fighter frames and the pre-explosion sphere behavior are still interpreted incorrectly.

I inspected the actual `0001_rasen*.png` and `0001_prasen*.png` artwork. The intended successful-hit animation is:

FORM
rasen1 -> rasen2 -> rasen3

RUSH
rasen4 -> rasen5 -> rasen6

CONTACT / HOLD
rasen7 -> rasen8
then HOLD rasen8 while the sphere stays attached, rotates, and grows larger and larger.

EXPLOSION
rasen9
The actual blast happens while #0001 is on rasen9.

POST-EXPLOSION RELEASE / RECOVERY
rasen10 -> rasen11 -> rasen12

That is the critical correction.

Do NOT keep rasen10/11 in the pre-explosion confirm sequence.
Do NOT hold rasen11 waiting for the explosion.
Do NOT use rasen12 during the explosion.
rasen9 is the explosion pose.
rasen10 is where the post-explosion release/recovery starts.

--------------------------------------------------
1. CORRECT THE FIGHTER ANIMATION SPLIT
--------------------------------------------------

Current `js/data/characters.js` has approximately:

rasenForm      = rasen1-3
rasenDash      = rasen4-6
rasenConfirm   = rasen7-11
rasenRelease   = rasen12

That is still wrong for a successful hit.

Change the logical clips to represent the actual artwork:

rasenForm:
  rasen1, rasen2, rasen3

rasenDash:
  rasen4, rasen5, rasen6

rasenConfirm:
  rasen7, rasen8

rasenExplosion:
  rasen9 only

rasenRelease:
  rasen10, rasen11, rasen12

A clean rush miss still needs the special behavior established previously:
the final visible miss/release pose should be rasen12.

Therefore it is fine to add a dedicated logical clip such as:

rasenWhiffRelease:
  rasen12 only

This reuses the same existing PNG. Do NOT duplicate, regenerate, rename, redraw, or modify the PNG file itself.

It is okay for `rasen12.png` to be referenced by two logical animation clips:
- the last frame of successful post-explosion `rasenRelease`;
- the one-frame `rasenWhiffRelease` for a failed rush.

The old test that assumes every rasen source path can occur in exactly one logical clip must be updated accordingly. What matters is that there is still exactly one physical PNG file on disk.

All fighter clips remain right-facing source art and one-shot at RASEN_FPS.

--------------------------------------------------
2. CORRECT THE SUCCESSFUL-HIT STATE MACHINE
--------------------------------------------------

The current successful path is roughly:

form
-> dash
-> confirm rasen7-11
-> wait holding rasen11
-> explode while showing rasen12
-> done

Replace that successful path with:

form
-> dash
-> confirm
-> wait
-> explode
-> release
-> done

Meaning:

FORM
- unchanged.

DASH
- unchanged.
- rasen4-6.
- fixed rush velocity.
- sphere carried in the hand.
- this remains the ONLY contact-search phase.

CONFIRM
- contact applies first hit exactly once.
- rush stops immediately.
- target becomes bound.
- target immediately displays Hurt/Mid-air Hurt on the same collision tick.
- sphere moves onto the enemy.
- #0001 plays rasen7 -> rasen8.
- sphere begins its attached rotation.

WAIT
- #0001 HOLDS rasen8.
- absolutely do not advance him to rasen9 yet.
- rasen9 belongs to the explosion itself.
- while rasen8 is held, the sphere continues rotating and gradually becomes larger and larger.
- the target remains bound/Hurt.
- #0001 remains locked and stationary.
- this lasts until the existing `explosionDelay`.

EXPLODE
- when the delayed explosion begins, immediately switch #0001 to rasen9.
- rasen9 is the fighter’s explosion pose.
- sphere stops using its normal attached-state visuals and switches to the authored explosion effect.
- explosion hit still happens on the first explosion frame.
- release the target from the bind BEFORE applying explosion knockback, as the code already does.
- hold rasen9 for the duration of the visible sphere explosion if necessary; do not start rasen10 until the explosion effect itself has finished.

RELEASE
- only AFTER the visible explosion completes:
  rasen10 -> rasen11 -> rasen12
- no sphere remains during this phase.
- no more damage.
- no contact search.
- no bind.
- no additional explosion.
- fighter remains committed/locked during this short recovery.
- after rasen12 finishes, technique ends and normal control returns.

This means the successful sequence on screen should read unmistakably as:

rasen7
rasen8
rasen8 held...
rasen8 held...
rasen8 held...
EXPLOSION -> rasen9
then
rasen10
rasen11
rasen12
then neutral.

Do not let rasen10 or rasen11 appear before the blast.

--------------------------------------------------
3. KEEP THE CLEAN-MISS BEHAVIOR SEPARATE
--------------------------------------------------

Do not accidentally make a whiff play the whole rasen10-12 successful recovery if that changes the intended failed-rush look.

The earlier requirement still applies:

If #0001 rushes and hits nobody:
- rush ends;
- velocity becomes 0;
- contact search ends;
- no enemy is damaged;
- no enemy is bound;
- no prasen7-11 attached/blast sequence occurs;
- the sphere disappears without exploding;
- #0001 displays the LAST rasen frame, rasen12, as the whiff release;
- hold it for the intended one-frame duration;
- then return to neutral.

A wall-stopped rush should use the same whiff-release semantics.

So a clean implementation can use separate data/runtime fields such as:

confirmAnimation
explosionAnimation
releaseAnimation
whiffReleaseAnimation

Do not hard-code #0001 filenames directly inside the generic runtime. Keep ChargedTechnique data-driven.

--------------------------------------------------
4. THE SPHERE MUST STILL ROTATE ON THE TARGET
--------------------------------------------------

Preserve the previous correction that made the attached blue sphere rotate.

The attached sphere should keep cycling:

prasen7 -> prasen8 -> prasen9 -> prasen7 -> ...

until the explosion starts.

Do not freeze on one frame.

Do not put prasen10 or prasen11 into this rotation loop.

prasen10 and prasen11 are the bright explosion frames.

Keep the attached sphere direction-neutral and centre-anchored.

Keep its centre following the target through the existing `targetOffset`, including an airborne caught enemy.

--------------------------------------------------
5. ADD REAL SPHERE GROWTH BEFORE THE EXPLOSION
--------------------------------------------------

Rotation alone is not sufficient.

While #0001 is stopped/holding rasen8, the sphere is supposed to become progressively bigger and bigger before exploding.

Implement explicit visual scale growth for the sphere while it is attached to the enemy.

Important: do NOT assume cycling prasen7-9 provides the growth.

The actual source PNG canvas sizes are approximately:

prasen7: 116 x 118 source pixels
prasen8: 106 x 110
prasen9: 100 x 106

So their native dimensions actually trend smaller rather than monotonically larger.

Therefore growth must be represented explicitly in the technique/rendering logic.

A clean implementation would expose a data-driven attached-sphere growth configuration on the technique definition, for example conceptually:

sphereGrowth: {
  startScale: ...,
  endScale: ...,
}

or equivalent.

Do not bury #0001-specific magic constants directly in Arena.drawTechnique.

The technique runtime should expose something like a visual `sphereScale` derived deterministically from its state/time, and the renderer should render the centered sphere with that scale multiplier.

Requirements for growth:

- sphere stays centred on the caught opponent;
- its centre must NOT drift as it becomes larger;
- it continues rotating through prasen7-9 while scaling;
- scaling affects visuals only;
- it must not create repeated hits;
- it must not enlarge/re-enable the rush hitbox after contact;
- it must not alter the target hurtbox or collision;
- growth must be deterministic under the fixed 60 Hz update;
- it should visibly increase continuously/progressively, not randomly pulse;
- scale should never shrink during this pre-explosion growth period.

Begin the growth once #0001 has reached the rasen8 hold portion of the contact sequence.

The visual should communicate:
sphere caught enemy -> energy keeps building -> sphere becomes increasingly large -> blast.

Use a configurable start/end scale rather than sprinkling renderer constants around the code.

Tune the final scale visually against the actual game so the growth is obvious but does not become absurdly oversized.

Add tests for the scale progression:
- scale at beginning of attached hold;
- scale partway through delay;
- scale immediately before explosion;
- assert start < middle < end;
- assert repeated calls at the same fixed-step time return identical values.

--------------------------------------------------
6. EXPLOSION VISUAL TIMING
--------------------------------------------------

The current sphere explosion art is:

prasen10
prasen11

Keep those as the explosion animation.

They are visibly much lighter/brighter than the attached sphere and represent the blast.

At exactly the configured explosion time:

1. #0001 switches from held rasen8 to rasen9.
2. attached sphere growth/rotation stops.
3. first sphere blast frame prasen10 appears.
4. target is released from the bind.
5. explosion hit is applied.
6. target receives the existing launch/knockback.
7. prasen11 follows once.
8. after the explosion animation finishes, the sphere disappears.
9. only then does #0001 begin rasen10 -> rasen11 -> rasen12 recovery.

Do NOT start rasen10 on the same instant that prasen10 first appears.

The explosion visual belongs with rasen9.

Because rasenExplosion may only contain one fighter frame while the sphere blast contains two frames, it is acceptable and preferable to hold rasen9 for the full sphere-explosion duration.

Then transition to release.

--------------------------------------------------
7. DAMAGE / COMBAT VALUES SHOULD NOT CHANGE
--------------------------------------------------

Unless another authoritative repo source explicitly contradicts them, preserve the existing combat values:

first contact:
- 4 damage;
- no launch;
- existing hitstun/blockstun/hitstop.

delayed explosion:
- 16 damage;
- existing 420 horizontal / 220 upward launch;
- existing stun/hitstop/blockstun.

Total unblocked successful move:
20 damage.

Keep exactly one contact damage event and exactly one explosion damage event.

Do not make the growing sphere deal repeated damage.

Do not turn the growth phase into a multihit.

--------------------------------------------------
8. IMMEDIATE HURT FRAME MUST REMAIN FIXED
--------------------------------------------------

The previous fix correctly forces the caught opponent into Hurt/Mid-air Hurt on the exact collision tick.

Do NOT regress this.

On the exact tick health changes 100 -> 96:
- target is already bound;
- target is already in Hurt or Mid-air Hurt;
- sphere is already centred on the target;
- attacker is already on the first contact pose;
- rush velocity is already stopped.

The successful first contact should begin at rasen7.

--------------------------------------------------
9. UPDATE THE GENERIC TECHNIQUE RUNTIME CLEANLY
--------------------------------------------------

Relevant file:

`js/game/charged-technique.js`

Do not special-case frame names like `"rasen8"` or `"rasen9"` inside the runtime.

Extend the definition schema so the phases remain data-driven.

Conceptually the definition now needs enough fighter animation roles to distinguish:

- formAnimation
- dashAnimation
- confirmAnimation
- explosionAnimation
- releaseAnimation
- whiffReleaseAnimation

along with the existing sphere animation roles.

Update `TECHNIQUE_DEFAULTS`, validation in `techniqueProblem`, constructor-resolved animation metadata/durations, and `animation` selection accordingly.

A clean explicit phase model would be:

form
dash
whiffRelease          // miss/wall only
confirm
wait
explode
release               // successful post-explosion recovery
done

Exact names can differ, but do not conflate these behaviors again.

Important duration rules:

confirm:
plays rasen7-8 once.

wait:
holds rasen8.

explode:
plays/holds rasen9 for the visible prasen10-11 blast duration.

release:
plays rasen10-12 once.

whiffRelease:
shows rasen12 for the intended short whiff recovery.

`velocityX` remains nonzero ONLY during dash.

--------------------------------------------------
10. RENDERER CHANGES
--------------------------------------------------

Relevant code:

`js/game/arena.js`
`Arena.drawTechnique()`

The renderer currently:
- asks the technique for its sphere frame;
- asks for sphere centre;
- draws the effect centred using the fighter art scale.

Keep that architecture.

Add the sphere growth multiplier cleanly.

For example, conceptually:

const scale = this.pxPerArt * t.sphereScale;

and pass the resulting art scale to the centered effect renderer.

Do not alter global fighter scale.
Do not resize the opponent.
Do not mutate normalized sprite frames.
Do not create new canvases each frame.
Do not rotate the entire canvas artificially; frame cycling already provides the authored sphere rotation.

The sphere must remain precisely centre-anchored as its scale increases.

During successful release rasen10-12 there should be no sphere to draw.

--------------------------------------------------
11. UPDATE TESTS THAT CURRENTLY ENCODE THE WRONG SEQUENCE
--------------------------------------------------

The current `tests/charged-ba2.test.mjs` explicitly encodes the previous interpretation and MUST be updated.

Current wrong expectations include things like:

- rasenConfirm = rasen7-11
- rasen11 held until the explosion
- rasen12 shown during the explosion
- successful blast and release happening in the same phase

Replace those expectations.

Add/modify tests to prove the corrected behavior.

A. Animation registration

Assert:

rasenForm      = 1-3
rasenDash      = 4-6
rasenConfirm   = 7-8
rasenExplosion = 9
rasenRelease   = 10-12

and whichever logical one-frame clip is used for whiff:
rasenWhiffRelease = 12

Do not require every source PNG to be referenced by exactly one logical clip, because rasen12 can legitimately be reused by the whiff clip.

Still verify no duplicate physical PNG files exist.

B. Contact / pre-explosion fighter frames

After contact:
- rasen7 appears;
- then rasen8;
- rasen8 remains held for the rest of the delay;
- rasen9 does NOT appear early;
- rasen10/11/12 do NOT appear early.

C. Sphere rotation

Before explosion:
prasen7 -> 8 -> 9 -> 7 -> ...
continues correctly.

D. Sphere growth

Sample attached-sphere scale at multiple points during the rasen8 hold:
- early;
- midpoint;
- immediately pre-explosion.

Assert monotonically increasing scale.

Also ensure its world/render centre stays the target centre despite the changing size.

E. Exact explosion transition

At the explosion tick:
- phase becomes explode;
- fighter frame is rasen9;
- sphere is prasen10;
- second damage event happens;
- target is released before knockback;
- target health becomes 80;
- launch remains intact.

On subsequent explosion visual frame:
- fighter still shows/holds rasen9;
- sphere advances to prasen11.

F. Post-explosion release

Only after prasen10-11 finish:
- sphere becomes null;
- phase enters release;
- rasen10 -> rasen11 -> rasen12 play once;
- no new damage events;
- controls remain locked until rasen12 completes;
- then technique clears and normal control returns.

G. Clean miss

Preserve:
- rasen12 is the whiff release pose;
- no prasen7-11;
- no damage;
- no bind;
- no successful-hit rasen7-11 sequence;
- then neutral.

H. Rendering test

Current Battle render test says the sphere spins while #0001 holds rasen11 and then expects rasen12 beside prasen10.

That test is obsolete.

Replace it so it verifies:
- target Hurt + rasen7 + prasen7 on contact;
- rasen8 held while the sphere rotates and gets progressively larger;
- rasen9 + prasen10 on explosion;
- rasen9 + prasen11 for the rest of the visible blast;
- then no sphere while rasen10, rasen11, rasen12 play.

If the fake canvas records destination width/height or transforms, explicitly assert that the rendered attached-sphere dimensions increase over the hold while its center translation stays unchanged.

--------------------------------------------------
12. UPDATE DOCUMENTATION
--------------------------------------------------

Update all stale descriptions in:

- `js/data/characters.js`
- `js/game/charged-technique.js`
- `tests/charged-ba2.test.mjs`
- `ALVA_SPEC.md`
- README/help text if they currently describe the old frame roles

Remove statements such as:

- rasen7-11 are all confirm frames;
- rasen11 is held until explosion;
- rasen12 is used as the explosion release pose.

Document instead:

rasen1-3   formation
rasen4-6   rush
rasen7-8   successful contact / stop
rasen8     held while attached sphere builds in size
rasen9     explosion pose
rasen10-12 post-explosion release/recovery
rasen12    also used alone as the clean-whiff release pose

prasen1-6  formation
prasen7-9  rotating attached sphere, visually scaled larger over the hold
prasen10-11 explosion

--------------------------------------------------
13. DO NOT REGRESS THE REST OF THE GAME
--------------------------------------------------

Preserve all existing behavior for:

- ordinary ground BA2;
- mid-air BA2;
- Charged BA1 Clone Attack;
- Throw;
- Dodge;
- Charge activation requirements;
- Energy;
- facing snapshot;
- left-facing mirroring;
- hand offsets;
- grounded-only startup;
- ground-loss cancellation;
- wall stopping;
- attacker being hit during the technique;
- Block interaction;
- Dodge interaction;
- knockout handling;
- reset/rematch cleanup;
- destroy cleanup;
- target bind ownership;
- airborne caught target falling/landing normally;
- immediate target Hurt pose;
- exactly two successful damage events;
- no extra hitboxes;
- fixed-step determinism.

Do not turn Sphere Rush into a normal attack/projectile/summon.
Do not rewrite unrelated systems.

--------------------------------------------------
14. VALIDATION
--------------------------------------------------

Run:

node --test tests/charged-ba2.test.mjs

Then:

node --test tests/*.test.mjs

Also manually inspect the move in the browser.

Successful hit must visibly read:

sphere forms
-> rush
-> contact on rasen7
-> settle on rasen8
-> #0001 remains frozen on rasen8
-> sphere continues rotating on the Hurt enemy
-> sphere steadily gets larger and larger
-> blast begins
-> #0001 is on rasen9 while prasen10-11 explode
-> sphere disappears
-> #0001 releases/recoveries through rasen10
-> rasen11
-> rasen12
-> neutral

Clean miss must visibly read:

sphere forms
-> rush
-> catches nobody
-> rush stops
-> no explosion
-> final rasen12 release pose
-> neutral

This is an animation-semantics fix, not a combat redesign. The finished move should make the role of each supplied sprite obvious from the gameplay instead of forcing the late rasen frames into the wrong phases.
