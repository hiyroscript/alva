You are modifying the current Alva repository:

hiyroscript/alva

This is a small corrective pass on top of the existing platform-fighter conversion.

The current repository already contains:

* accumulated Knockback;
* 3-point elimination scoring;
* 2-second respawns;
* Dash;
* stamina used by Dash/Dodge/Block;
* stamina passive regeneration;
* faster stamina regeneration while Charging;
* exhausted stamina state;
* CAB1/CAB2 charged cooldowns;
* fighter-attached CAB cooldown indicators;
* fighter-attached stamina bar;
* current fighter facing logic.

Do not redesign these systems.

For this pass, change only:

1. CAB1/CAB2 cooldown indicators should appear only while that ability is actually on cooldown.
2. The stamina/energy-style purple bar should appear only while it is refilling / not full.
3. Once stamina reaches 0, it should remain visually gray while refilling and Dash/Dodge/Block must remain locked until it reaches full.
4. Remove the behavior where fighters automatically turn to face each other.
5. Preserve all other current combat, scoring, Dash, Knockback, respawn, and HUD behavior.

⸻

1. Current implementation details

Relevant files include:

* js/game/character.js
    * Fighter movement
    * Dash
    * Charge
    * facing updates
    * stamina recovery
* js/game/combat.js
    * CombatState
    * stamina state
    * exhausted state
    * charged cooldown timers
* js/game/arena.js
    * world-space stamina/CAB rendering
* js/game/hud.js
    * top character cards
* js/data/characters.js
    * #0001 movement/stamina data
* tests covering Dash, stamina, cooldowns, and facing.

Do not reintroduce old Health or old Energy.

For player-facing discussion in this pass, the purple resource may still be referred to as the stamina/action-energy bar, but keep the current implementation naming if it is already established as stamina.

⸻

2. CAB1/CAB2 indicators only appear during cooldown

The world-space CAB indicators currently represent:

* CAB1
* CAB2

They should not be permanently visible.

Desired behavior:

Ability ready

If CAB1 is ready:

* show nothing for CAB1.

If CAB2 is ready:

* show nothing for CAB2.

If both are ready:

* show no CAB indicators at all beneath the fighter.

There should not be:

* completed ready rings;
* permanent CAB1 text;
* permanent CAB2 text;
* empty placeholders.

The UI is purely a temporary cooldown notification.

⸻

3. CAB indicator appears immediately on use

When a charged ability successfully enters cooldown:

show its indicator immediately.

Example:

CAB1 used.

Under fighter:

CAB1
4.9

with its white/black-outlined circular cooldown progress.

As the timer decreases, continue displaying it.

When cooldown reaches exactly ready:

remove the indicator completely.

Do not leave it visible for an extra frame unnecessarily.

⸻

4. CAB indicators remain independent

If only CAB1 is cooling down:

show only CAB1.

If only CAB2 is cooling down:

show only CAB2.

If both are cooling down:

show both.

Example:

CAB1    CAB2

If CAB1 finishes first:

CAB1 disappears and CAB2 remains.

Do not reserve invisible layout slots that create strange gaps.

The remaining cooldown may recenter beneath the fighter if appropriate.

⸻

5. CAB cooldown data remains unchanged

Do not modify:

* CAB1 cooldown duration;
* CAB2 cooldown duration;
* independent timer behavior;
* faster recovery while Charging;
* cooldown reset on respawn;
* cooldown consumption rules.

This pass changes visibility only.

Use:

combat.chargedCooldowns.active(id)

or equivalent existing cooldown state.

Do not infer visibility from formatted remaining text.

⸻

6. CAB ready state should not render

If:

chargedCooldowns.remaining(id) <= 0

or the timer entry no longer exists:

do not render that ability’s world-space cooldown UI.

This should happen naturally because CooldownTimers.update() already removes entries when they finish.

Prefer rendering from active entries rather than rendering every charged action and making ready ones transparent.

That reduces unnecessary clutter.

⸻

7. Stamina bar visibility

The purple stamina bar above the fighter should not be permanently visible.

It should only be visible while the stamina resource is not full.

Desired:

Full stamina

stamina === maxStamina

Bar:

hidden

No purple full bar above the character.

Stamina spent

As soon as Dash/Dodge/Block spends stamina:

bar appears.

Example:

100 → 75

bar appears at 75%.

While regenerating

The bar remains visible while stamina rises back toward full.

Fully refilled

Once stamina reaches full:

bar disappears again.

⸻

8. Stamina bar visibility condition

Use the actual resource state.

Conceptually:

showStamina = stamina < maxStamina

with floating-point tolerance matching the rest of the fixed-step code.

Do not use arbitrary timers like:

“hide bar 1 second after action.”

The bar reflects actual resource state.

Because CombatState.setStamina() already snaps near-max values to the exact maximum, visibility should be straightforward.

⸻

9. Empty stamina behavior

The resource is allowed to reach:

0

completely.

Do not enforce:

* a 1-point minimum;
* an invisible reserve;
* automatic bounce to 1.

At zero:

* stamina = 0;
* staminaExhausted = true.

The fighter cannot use stamina-dependent actions.

⸻

10. Exhausted bar becomes gray

Once stamina reaches zero, the stamina bar switches from purple to:

gray

This exhausted appearance remains active during the entire refill process.

Example:

* 0/100 → gray
* 20/100 → still gray
* 60/100 → still gray
* 99/100 → still gray
* 100/100 → exhaustion clears and bar disappears.

Do not change it back to purple early.

⸻

11. Full-refill lockout

The existing mechanic already mostly implements this and must remain exact.

Once the fighter has exhausted the bar:

Dash:

* unavailable.

Dodge:

* unavailable.

Block:

* unavailable.

This remains true even after stamina regenerates partially.

Examples:

At 10:

* unavailable.

At 50:

* unavailable.

At 99:

* unavailable.

Only after:

stamina === maxStamina

should:

staminaExhausted = false

and the actions become available again.

Do not change this to a normal “if enough stamina, use ability” system after exhaustion.

The entire purpose of exhaustion is a forced full recharge.

⸻

12. Bar disappears when exhaustion ends

When the gray refill reaches full:

1. stamina snaps to max;
2. exhausted becomes false;
3. Dash/Dodge/Block become usable again;
4. stamina bar disappears.

Do not briefly display a full purple bar after recovery.

Full resource means hidden.

⸻

13. Non-exhausted partial refill remains purple

If stamina has been spent but has not reached zero:

example:

100 → Dash → 75

the fighter is not exhausted.

Bar should be:

purple

while it regenerates:

75 → 80 → 90 → 100.

At 100:

* hide.

Gray is specifically reserved for an exhaustion cycle caused by reaching zero.

⸻

14. Charging behavior remains unchanged

Charging still increases stamina regeneration speed.

The visibility logic simply follows the value.

Example:

fighter has 40 stamina.

Bar visible.

Player Charges.

Stamina regenerates faster.

Bar visibly fills faster.

At full:

* bar disappears.

If the fighter had previously exhausted stamina:

the faster Charge regeneration still fills the gray bar until full.

Do not clear exhaustion simply because Charge is being held.

⸻

15. Stamina spending remains unchanged

Do not modify current costs unless required to fix a bug.

Preserve existing configured values for:

* Dash;
* Dodge;
* Block drain.

Do not make CAB1/CAB2 consume stamina.

Do not make:

* normal attacks;
* jumps;
* running;
* shuriken;
* Charge

consume stamina.

⸻

16. Hide stamina while fighter is not active

Continue hiding stamina UI for:

* eliminated fighters;
* fighters during their 2-second respawn absence;
* fighters not currently rendered;
* any fighter intentionally detached from active play.

After respawn:

stamina starts full.

Therefore:

* bar should initially be hidden.

It appears again only after stamina is spent.

⸻

17. Remove automatic opponent-facing

The fighter currently has facing logic that automatically turns a grounded idle fighter toward its opponent.

Remove this behavior entirely.

A fighter should not automatically look toward the enemy.

Facing should now be controlled only by the fighter’s own movement/action direction or explicit gameplay mechanics.

⸻

18. Remove opponent-based facing from updateFacing()

Inspect Fighter.updateFacing().

The current behavior includes logic conceptually like:

* if moving, face movement direction;
* otherwise, while grounded and idle, look at opponent based on opponent X position.

Remove the opponent-position branch.

Do not replace it with another target-tracking system.

The fighter should retain its last facing while stationary.

⸻

19. Desired facing behavior

Example:

P1 is facing right.

Enemy jumps behind P1 to the left.

P1 remains facing:

right

until P1 performs an input/action that explicitly changes facing.

No automatic turn.

⸻

20. Movement changes facing

Normal left/right movement should continue to control facing.

If player deliberately moves right:

face right.

If player deliberately moves left:

face left.

Keep the current threshold logic if it is useful for avoiding sprite flicker.

But opponent position must not influence it.

⸻

21. Dash changes facing

Existing Dash behavior should continue to face the Dash direction.

Right Dash:

* face right.

Left Dash:

* face left.

Do not alter this.

⸻

22. Attack facing behavior

Attacks should continue to use the fighter’s current facing.

Do not automatically rotate toward the opponent when an attack starts.

Example:

fighter faces right;
opponent is left;
player presses BA1 without first turning.

BA1 should attack:

right

This is expected under the new manual-facing rule.

⸻

23. Throw behavior

Shuriken should continue to travel in the fighter’s current facing.

No aim correction toward enemy.

If facing right:

* shuriken travels right.

Even if opponent is left.

⸻

24. Charged abilities

Charged attacks should also respect current facing.

Charged BA2/Sphere Rush:

* should snapshot the fighter’s current facing as it already does.

Do not rotate toward opponent before it starts.

Charged BA1 clone placement may still be based on opponent position because that move specifically summons relative to its target.

That is not fighter auto-facing.

Do not break target-dependent summon placement.

⸻

25. CPU facing

The Practice/Training CPU should follow the same generic Fighter facing rules.

Do not automatically make the dummy turn to face P1.

If its AI moves:

* its movement may update its facing.

If it stops:

* it keeps its last facing.

This is intentional.

Do not add a CPU-only auto-look exception.

⸻

26. Respawn facing

On respawn:

use the stage spawn’s configured:

spawn.facing

exactly as currently defined.

After respawn, the fighter retains that facing until movement/Dash/etc. changes it.

Do not immediately turn toward opponent after appearing.

⸻

27. Preserve HUD portrait inward-facing rule

This change applies to the actual fighter sprite, not the HUD profile portraits.

The top HUD portraits should continue to face inward toward the timer as previously requested.

Therefore:

* gameplay fighter facing = manual;
* HUD portrait orientation = fixed UI composition.

Do not couple those two.

⸻

28. CAB layout when hidden

Because cooldown indicators now exist only when active, make the rendering layout dynamic.

Examples:

No cooldowns

Render nothing.

Only CAB1

Center CAB1 beneath fighter.

Only CAB2

Center CAB2 beneath fighter.

Both

Place side-by-side.

Avoid leaving an empty invisible CAB1 slot when only CAB2 exists.

⸻

29. Stamina/CAB vertical spacing

Because both status systems are conditional:

* stamina may be hidden;
* CABs may be hidden.

Do not reserve giant fixed empty gaps around the fighter.

World-space layout should collapse cleanly.

Above fighter:

if stamina visible:

* draw it.

Otherwise:

* marker can occupy its normal position.

Below fighter:

if CAB cooldown active:

* draw indicators.

Otherwise:

* draw nothing.

⸻

30. Do not modify top info boxes

Do not move CAB cooldown information back into:

* top fighter card;
* portrait;
* character-name box.

The previous requirement remains:

cooldowns belong beneath the actual fighter.

This pass only changes when they are visible.

⸻

31. Tests: cooldown visibility

Add/update tests verifying:

* CAB1 ready → no CAB1 indicator rendered;
* CAB2 ready → no CAB2 indicator rendered;
* both ready → no cooldown UI under fighter;
* CAB1 used → CAB1 appears;
* CAB2 remains absent if ready;
* CAB2 used while CAB1 cooling → both appear;
* CAB1 finishing removes only CAB1;
* final cooldown finishing removes all cooldown UI.

Test state/renderer calls rather than brittle screenshots.

⸻

32. Tests: stamina visibility

Verify:

fresh fighter:

* stamina full;
* no stamina bar rendered.

After Dash:

* stamina below full;
* bar rendered.

After Dodge:

* bar rendered.

During regen:

* bar remains rendered.

At full:

* bar no longer rendered.

⸻

33. Tests: purple normal refill

Verify:

* spend stamina without reaching zero;
* staminaExhausted === false;
* bar uses purple normal style;
* bar remains purple through partial refill;
* disappears at full.

⸻

34. Tests: gray exhaustion cycle

Verify:

1. stamina reaches exactly 0;
2. staminaExhausted === true;
3. bar visible and gray;
4. refill to 25 → still gray;
5. refill to 75 → still gray;
6. refill to 99 → still gray;
7. reach 100 → exhausted false;
8. bar hidden.

Do not allow purple before step 7.

⸻

35. Tests: exhausted ability lock

Verify while exhausted at partial stamina:

* Dash fails;
* Dodge fails;
* Block fails.

At full:

* they become available again.

Existing tests may already cover part of this; update only where needed.

⸻

36. Tests: no opponent auto-facing

Create clear facing tests.

Example:

fighter starts facing right.

Opponent moves from right side to left side.

Fighter gives no movement input.

After updates:

fighter still faces right.

⸻

37. Tests: movement facing

Verify:

* deliberate left movement changes facing left;
* deliberate right movement changes facing right;
* stopping preserves last direction;
* opponent position does not alter it afterward.

⸻

38. Tests: attack direction

Verify:

* fighter facing right with opponent on left;
* BA1 starts;
* hitbox remains on right side;
* no auto-turn occurs.

Do similar lightweight coverage for projectile direction if useful.

⸻

39. Tests: Dash facing

Verify existing behavior still holds:

* left Dash faces left;
* right Dash faces right;
* opponent position does not override Dash facing.

⸻

40. Tests: respawn facing

Verify:

* respawn uses configured spawn facing;
* opponent’s location does not immediately flip it.

⸻

41. Code comments/docs

Update stale comments that say:

* ready cooldowns remain visible;
* stamina bar is persistent;
* fighters automatically face opponent.

Document:

* cooldown UI appears only while cooling;
* stamina UI appears only below full;
* exhausted refill is gray until fully restored;
* fighters retain facing until their own directional action changes it.

Keep documentation edits narrowly scoped.

⸻

42. Preserve existing systems

Do not change:

* 3-point scoring;
* score dots;
* 2-second respawn;
* Practice default CPU;
* Practice CPU info card;
* Knockback accumulation;
* damage tuning;
* Void behavior;
* Dash timing/speed;
* stamina costs;
* stamina regen rates;
* CAB cooldown durations;
* Charge cooldown acceleration;
* Charge stamina acceleration;
* stage layouts;
* camera framing.

⸻

43. Acceptance criteria

This corrective pass is complete when:

CAB cooldown indicators

* ready abilities show nothing;
* only cooling abilities appear;
* indicator disappears immediately when cooldown completes;
* CAB1 and CAB2 remain independent;
* indicators stay under actual fighter.

Stamina bar

* full resource = no bar;
* spending resource makes bar appear;
* ordinary partial refill = purple;
* zero stamina = exhausted;
* exhausted bar = gray;
* gray remains through partial refill;
* stamina-dependent actions locked throughout exhaustion;
* only full refill clears exhaustion;
* full refill causes bar to disappear.

Facing

* fighters no longer automatically face opponents;
* idle fighter preserves last facing;
* movement changes facing;
* Dash changes facing;
* attacks use current facing;
* projectiles use current facing;
* respawn uses configured spawn facing;
* HUD portraits still independently face the timer.

Quality

* no duplicate/hidden cooldown placeholders;
* no permanent stamina bar;
* no automatic target tracking remains in fighter facing;
* existing tests pass;
* add focused regression tests for these changes.

⸻

44. Strict scope

Implement only:

* cooldown indicators visible only during active cooldown;
* stamina bar visible only while below full;
* gray exhausted refill until full;
* retain current full-refill lockout;
* remove automatic fighter-to-opponent facing;
* directly affected tests/comments.

Do not expand beyond this scope.
