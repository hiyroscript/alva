You are modifying the hiyroscript/alva repository. Before changing anything, inspect the existing implementation carefully and preserve the architecture, visual language, accessibility, navigation conventions, and existing gameplay behavior.

Also inspect hiyroscript/seren, specifically its Cars & more reference screen. Use the composition and information architecture of that screen as inspiration, but do NOT copy Seren’s visual identity. The new screen must look and feel native to Alva.

Goal

Add a new reusable Power system to Alva, beginning with Jump Power, and add a new Discover reference screen accessible from Home.

There are three Jump Power tiers:

1. Jump Power 1 — very low jump.
2. Jump Power 2 — normal jump. This must be exactly the jump #0001 already has today.
3. Jump Power 3 — slightly higher than Jump Power 2.

#0001 must use Jump Power 2.

The work must create an actual extensible gameplay/data system. Do not merely rename jumpVelocity in the UI.

Current Alva behavior that must be preserved

The repository currently defines global gravity in js/config.js as:

CONFIG.sim.gravity = 2500

#0001 currently has this movement value in js/data/characters.js:

jumpVelocity: 920

Jumping is applied centrally in js/game/character.js through the equivalent of:

body.vy = -mv.jumpVelocity

The existing buffered jump, coyote time, grounded checks, animation selection, combat/action gating, CPU behavior, gravity, collision, falling, landing, platforms, and input behavior must remain intact.

Jump Power 2 must resolve to exactly 920 world units/second, so #0001 feels identical after this feature ships.

Do not alter global gravity to implement the tiers.

Jump Power data model

Create a single source of truth for Power definitions rather than scattering numerical jump velocities throughout fighter definitions.

Prefer a dedicated data module such as:

js/data/powers.js

Create a Jump Power tier table with three entries.

Use these initial tuning values:

* Jump Power 1 → 650
* Jump Power 2 → 920
* Jump Power 3 → 1000

At Alva’s existing gravity, this makes Power 1 dramatically lower than the current jump, Power 2 exactly the existing jump, and Power 3 noticeably but only slightly higher.

The names, descriptions, tier numbers, and gameplay values should come from this central definition wherever practical.

A good conceptual structure is:

JUMP_POWER_TIERS

with entries containing the tier, display name, description, and jump velocity.

Also expose a small resolver/helper rather than having Fighter know the table structure directly.

For example, conceptually:

getJumpPowerTier(characterDefinition)

and/or

getJumpVelocityForCharacter(characterDefinition)

Choose names that fit the existing codebase.

Character schema

Give fighters an explicit Power configuration.

For #0001, add something conceptually equivalent to:

powers: { jump: 2 }

Do not represent #0001 as a special case in the physics code.

The point is that future fighters can simply declare:

powers: { jump: 1 }

or:

powers: { jump: 3 }

and automatically receive the corresponding gameplay jump.

The fighter definition is responsible for saying which power tier it owns. The Power system is responsible for translating that tier into the actual gameplay value.

Avoid maintaining two independent canonical values such as both powers.jump: 2 and an unrelated movement.jumpVelocity: 920. That can drift later. After the migration, the tier system should be the source of truth for voluntary jump velocity.

If a defensive fallback is necessary for malformed character data, make it explicit and safe, but valid fighters should always use the Power system.

Gameplay integration

Update the shared fighter jump implementation so that when a valid jump begins it receives its vertical velocity from the fighter’s Jump Power tier.

Everything around the jump must continue to work exactly as it does now:

buffering,
coyote time,
canAct(),
ground checks,
charge interruption,
blocking interruption,
jump/fall/land animation states,
platform interaction,
ceiling collision,
CPU jumps,
Practice Ground,
Quick Battle,
touch controls,
keyboard controls,
and controllers.

Do not create separate Player and CPU implementations.

The shared Fighter behavior should make the tier work everywhere automatically.

Only the initial voluntary jump strength should change between the tiers.

Knockback, launches caused by attacks, falling, gravity, hitstun, charged techniques, dodges, and other vertical motion must not use Jump Power unless they are genuinely performing the standard jump action.

#0001 requirement

Set #0001 to:

Jump Power 2

This must preserve its current jump exactly.

A fresh #0001 jump should therefore still begin at:

body.vy === -920

before gravity is integrated.

There should be no gameplay regression for #0001 from introducing this system.

Home — add Discover

Alva’s current Home navigation is built in:

js/screens/home-screen.js

It currently contains:

Play
Practice Ground

Add a third Home action immediately under Practice Ground:

Discover

So the ordering becomes:

Play
Practice Ground
Discover

Use the same home-action component/style and the same right-chevron treatment as Practice Ground. It must participate in existing keyboard/gamepad menu navigation through data-nav.

Clicking Discover opens the new Discover screen directly.

Do NOT put Discover inside the Practice Ground pause/More menu. It is a Home destination directly below Practice Ground.

New Discover screen

Create a proper screen using Alva’s existing Screen / ScreenManager architecture, for example:

js/screens/discover-screen.js

Register it in js/core/app.js.

Add its corresponding <section> to index.html, following Alva’s existing screen structure, for example with:

data-screen="discover"

and an appropriate accessible label.

The screen must support normal ScreenManager enter/exit behavior and return cleanly to Home.

Discover visual direction

Use Seren’s Cars & more screen as structural inspiration.

Relevant Seren ideas include:

a top header with a Back control and page title,
a compact reference/index rail,
the active section visually indicated on that rail,
a scrollable reference content area,
structured informational cards,
two-column content where space permits,
one-column content on smaller layouts,
and a horizontal tab rail on narrow screens.

However, this is an Alva screen.

Do not use Seren’s red accents or SEREN typography/look.

Use Alva’s existing visual tokens from styles.css:

near-black / charcoal surfaces,
off-white text,
Alva’s restrained green --accent,
--accent-hi,
--accent-wash,
existing borders,
existing focus treatment,
existing radius system,
existing spacing,
existing screen header/back-button patterns.

The result should look like Discover was always part of Alva.

Avoid introducing an unrelated design system.

Discover header

The screen title should be:

Discover

Provide a Back button using Alva’s existing icon/button language.

Back returns to Home.

It must work with pointer, keyboard, gamepad/menu navigation, and the existing Back/Escape behavior where appropriate.

Discover sections

The Discover screen currently has exactly two sections:

POWER

CONDITIONS

POWER comes first and is selected by default.

Structurally this should feel inspired by Seren’s Cars & more left-hand index rail:

POWER
CONDITIONS

On narrow/mobile layouts, convert this into a horizontal rail similar to Seren’s responsive reference screen.

Use Alva green for the active indicator rather than Seren red.

The tabs/section controls must be real accessible buttons.

Use sensible role="tablist", role="tab", aria-selected, and focus behavior if implementing it as a tab interface.

Arrow-key navigation between POWER and CONDITIONS is desirable and should follow the existing menu/navigation architecture without fighting it.

POWER content

POWER is a reference page explaining gameplay Powers.

For this first version it only contains Jump Power.

Create a reference entry headed:

JUMP POWER

Explain clearly and briefly that Jump Power controls how high a fighter’s normal jump goes and that higher tiers jump higher.

Then show the three tiers in a clean Alva-style reference treatment:

JUMP POWER 1
Very low jump.

JUMP POWER 2
Normal jump.

JUMP POWER 3
Slightly higher jump.

Clearly indicate that #0001 uses Jump Power 2.

This information should be driven from the same Jump Power tier data used by gameplay as much as reasonably possible. Do not create a completely separate hard-coded UI definition of what the three tiers mean if the gameplay table already contains their names/descriptions.

Do not expose raw 650, 920, or 1000 velocity numbers to the player in the Discover UI unless Alva already establishes a reason to show low-level tuning numbers. The screen should communicate the game concept, not developer physics constants.

The Discover content should remain easy to extend later with additional Power types.

For example, a future developer should be able to add another Power category without redesigning the whole screen.

CONDITIONS content

The second section is:

CONDITIONS

For now, Conditions intentionally has no content.

Do not add fake Conditions.

Do not write “Coming soon.”

Do not add placeholder cards.

Do not invent descriptions.

When CONDITIONS is selected, the content region can simply be empty while the section/tab remains fully functional and accessible.

This is intentional scaffolding for a future Conditions system.

Styling

Add Discover-specific styling to styles.css while reusing existing Alva primitives and CSS variables.

Do not blindly paste Seren CSS.

Create Alva-specific class names, such as a discover-* namespace or whatever matches the current conventions.

Desktop/wide layout should approximately follow this composition:

Back + Discover header across the top.

Below it, a narrow left reference rail containing POWER and CONDITIONS.

To its right, a scrollable content region.

The POWER reference content should have restrained dividers/cards similar in informational density to Seren’s Cars & more cards, but styled with Alva’s existing dark surfaces and green active state.

At narrower widths, place POWER and CONDITIONS in a horizontal rail above the content.

Respect safe-area insets.

Keep the screen usable at short landscape heights because Alva is designed around landscape play.

Ensure long content can scroll without making the entire app/document scroll unexpectedly.

Accessibility

Preserve Alva’s accessibility conventions.

Discover and every interactive control must be keyboard reachable.

Focus must always be visible.

Hidden section content must not remain focusable.

The currently selected POWER/CONDITIONS control must expose its state semantically.

The Back control needs a meaningful accessible name.

Do not rely on green colour alone to communicate the active section; use structure/border/state as well.

Respect reduced-motion behavior. This screen does not need elaborate animations.

Tests

Update and add tests rather than weakening existing tests.

tests/practice-ground.test.mjs currently verifies that Home has exactly two Home actions and navigation candidates [Play, Practice Ground]. Update this expectation for:

Play
Practice Ground
Discover

Also test that clicking Discover navigates to the Discover screen, while Practice Ground still opens Practice Ground directly.

Add focused tests for the Power system.

At minimum verify:

Jump Power 1 resolves to 650.

Jump Power 2 resolves to 920.

Jump Power 3 resolves to 1000.

The values are strictly ordered:

Power 1 < Power 2 < Power 3.

#0001 declares Jump Power 2.

A normal #0001 jump still starts at exactly -920 vertical velocity.

A fighter configured with tier 1 receives the tier-1 jump.

A fighter configured with tier 3 receives the tier-3 jump.

Tier changes do not change gravity, max fall speed, coyote time, jump buffering, or unrelated movement stats.

The same Fighter implementation handles player-controlled and CPU-controlled fighters.

Add a Discover screen test covering at least:

screen registration/construction,
Home → Discover,
Back → Home,
POWER selected by default,
POWER shows Jump Power information,
#0001 is identified with Jump Power 2,
CONDITIONS is selectable,
CONDITIONS contains no invented placeholder content,
and keyboard/menu navigation can reach the controls.

Do not make tests dependent on pixel-perfect visual layout.

Documentation

Update the appropriate Alva documentation (README.md, ALVA_SPEC.md, or both where appropriate) so the architecture describes:

the new Power system,
the three initial Jump Power tiers,
#0001 using Jump Power 2,
the fact that Jump Power 2 preserves the original 920 jump,
and the Discover reference screen.

Avoid leaving documentation saying fighter jump strength is only a raw movement.jumpVelocity if that is no longer true.

Files likely involved

Expect to inspect and potentially modify:

index.html

styles.css

js/core/app.js

js/screens/home-screen.js

js/screens/discover-screen.js — new

js/data/powers.js — new or equivalent

js/data/characters.js

js/game/character.js

tests/practice-ground.test.mjs

new focused Power/Discover tests

README.md

ALVA_SPEC.md

Do not assume this list is exhaustive. Follow actual dependencies in the repository.

Non-goals

Do not add selectable Power upgrades.

Do not let the player change #0001’s Jump Power from Discover.

Do not add progression/unlocking.

Do not add Conditions gameplay yet.

Do not change attack power, damage, speed, gravity, defense, Energy, or other fighter stats.

Do not rebalance #0001’s existing normal jump.

Do not redesign Home.

Do not replace Practice Ground.

Do not copy Seren’s red palette.

Do not create a second independent jump implementation for Practice Ground or CPUs.

Definition of done

The implementation is complete when Home displays Discover directly below Practice Ground, Discover opens a polished Alva-native reference screen inspired structurally by Seren’s Cars & more screen, POWER and CONDITIONS are available with POWER selected by default, POWER accurately explains the three Jump Power tiers, CONDITIONS is intentionally empty, the gameplay uses a centralized three-tier Jump Power system, #0001 is assigned Jump Power 2, and #0001’s actual current jump remains exactly unchanged.

Run the full existing Node test suite plus all new tests and fix regressions rather than changing expected behavior to hide them. Also manually verify Home, Discover, POWER/CONDITIONS switching, Back behavior, #0001 jumping in Practice Ground, #0001 jumping in Quick Battle, keyboard navigation, gamepad/menu navigation, and narrow landscape responsive layout.
