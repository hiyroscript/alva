Work in the hiyroscript/alva repository.

Task

Change the Select Stage primary action text from:

Confirm stage and Start Battle

to exactly:

Confirm and start battle

The final visible button text must match that wording and capitalization exactly.

Product context

Alva is a browser-based 2D fighting game built with plain HTML, CSS, and vanilla JavaScript ES modules.

The main menu/game flow is:

Splash → Home → Select Mode → Select Fighter → Select Stage → Battle

The requested text appears on the Select Stage screen as the primary action used to enter Battle after choosing a stage.

The Select Stage implementation is in:

js/screens/map-select-screen.js

The relevant button is currently created as this.startBtn with:

html: `<span>Confirm stage and Start Battle</span>${ICONS.arrow}`,

Its click handler calls the existing start() method, which transitions to the Battle screen using the selected map and character.

Required implementation

In js/screens/map-select-screen.js, change only the button copy so it becomes:

html: `<span>Confirm and start battle</span>${ICONS.arrow}`,

Preserve the existing:

* btn btn--primary btn--start classes
* type="button"
* data-nav
* arrow icon
* click listener
* start() behavior
* selected stage behavior
* keyboard/gamepad menu navigation
* mouse/touch interaction
* focus behavior
* accessibility semantics
* layout and styling

Do not rename the button variable, restructure the component, or modify the transition into Battle.

Keep the product specification synchronized

ALVA_SPEC.md is explicitly documented as the single product specification for Alva, and its Select Stage section currently specifies:

"Confirm stage and Start Battle" primary action

Update that specification to use the new exact copy:

"Confirm and start battle" primary action

There is also a general typography rule in ALVA_SPEC.md stating that headings and buttons use title case. The requested label is intentionally sentence case. Do not change the requested text to satisfy that general rule.

If necessary to keep the specification internally consistent, make only the smallest documentation adjustment needed to identify this Select Stage action as a deliberate sentence-case exception. Do not broadly redesign or rewrite Alva’s typography rules.

Scope constraints

This is a copy-only UI change.

Do not change:

* stage selection logic
* Desert or City data
* stage previews or Canvas rendering
* Battle loading or navigation
* fighter selection
* menu navigation
* button dimensions or CSS
* colors, typography styling, or icons
* combat, physics, camera, HUD, or game controls
* any unrelated text

Do not introduce dependencies, frameworks, or new abstractions for this change.

Repository-wide verification

Search the repository for the exact old phrase:

Confirm stage and Start Battle

After the change, there should be no remaining user-facing or authoritative-spec occurrences of that old wording unless an occurrence is intentionally historical and should remain unchanged.

Do not blindly replace unrelated text.

Validation

Verify that:

1. The Select Stage primary button visibly reads exactly:
    Confirm and start battle
2. The capitalization is exact:
    * Confirm begins with a capital C.
    * and start battle remains lowercase.
3. The arrow icon still appears in the button.
4. Clicking the button still starts Battle with the currently selected stage and fighter.
5. Existing keyboard, gamepad, mouse, and touch behavior is unchanged.
6. Stage selection still works normally for both Desert and City.
7. ALVA_SPEC.md reflects the new label.
8. No unrelated visual or functional changes are introduced.
9. Existing tests, if applicable, still pass.

Deliverable

Make the smallest clean diff necessary for this request.

When finished, report:

* the files changed
* the exact copy change made
* whether any specification wording needed adjustment for the intentional sentence-case exception
* what validation/tests were performed

Do not make any unrelated improvements or refactors.
